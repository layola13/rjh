/**
 * Asset module that exports the path to the HID SVG icon.
 * This module is typically used for importing static SVG assets in a webpack-bundled application.
 */

/**
 * The public path to the HID SVG asset.
 * This path is resolved at build time and points to the optimized SVG file.
 * 
 * @example
 *
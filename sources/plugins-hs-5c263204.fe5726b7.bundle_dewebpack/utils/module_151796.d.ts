/**
 * CSS module type definitions
 * 
 * This module represents a CSS module loader that processes stylesheets
 * and injects them into the DOM. It exports CSS class name mappings.
 */

/**
 * CSS class name mappings exported by the CSS module.
 * Maps local class names to their hashed/scoped equivalents.
 * 
 * @example
 *
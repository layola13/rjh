/**
 * Webpack asset module that exports the path to a scroll mask PNG image.
 * This file represents a static asset import that Webpack processes and places in the build output.
 * 
 * @module ScrollMaskAsset
 */

/**
 * The resolved public path to the scroll mask image asset.
 * This path is computed by Webpack during the build process and includes
 * the content hash (31790b74) for cache busting.
 * 
 * @example
 *
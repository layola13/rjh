/**
 * Retrieves the 3D image metadata from the application's design configuration.
 * 
 * This function accesses the global HSApp instance to fetch design metadata
 * specifically for 3D images. The metadata structure depends on the application's
 * design system configuration.
 * 
 * @returns The 3D image design metadata object, or undefined if not configured
 * 
 * @example
 *
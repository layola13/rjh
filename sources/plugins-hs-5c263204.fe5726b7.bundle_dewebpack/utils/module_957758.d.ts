/**
 * Asset path resolver for recommendation arrow hover icon.
 * 
 * This module exports the static asset path for the recommendation arrow hover SVG file.
 * The path is resolved relative to the public path configured in the build system.
 * 
 * @module AssetPath
 */

/**
 * The resolved public path to the recommendation arrow hover SVG asset.
 * 
 * This path includes the content hash (5da38395) for cache busting purposes.
 * 
 * @example
 *
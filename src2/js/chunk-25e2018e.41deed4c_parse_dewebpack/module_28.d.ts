/**
 * Compares two values to determine if the first is less than the second.
 * 
 * @template T - The type of values being compared (must support < operator)
 * @param first - The first value to compare
 * @param second - The second value to compare
 * @returns True if the first value is less than the second, false otherwise
 * 
 * @example
 *
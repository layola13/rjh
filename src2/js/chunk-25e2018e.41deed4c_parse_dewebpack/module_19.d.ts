/**
 * Bitwise NOT operator wrapper function.
 * Performs a bitwise NOT operation on the input number, which inverts all bits.
 * 
 * Note: In JavaScript, ~n is equivalent to -(n + 1) for integers.
 * Common use case: ~array.indexOf(x) to check if x exists in array (returns truthy if found).
 * 
 * @param value - The numeric value to apply bitwise NOT operation
 * @returns The bitwise complement of the input value
 * 
 * @example
 *
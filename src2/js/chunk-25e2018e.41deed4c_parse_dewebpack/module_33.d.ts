/**
 * Returns the first truthy value between two parameters.
 * Implements a logical OR operation that returns the first operand if it's truthy,
 * otherwise returns the second operand.
 * 
 * @template T - Type of the first parameter
 * @template U - Type of the second parameter
 * @param first - The first value to evaluate
 * @param second - The second value to return if first is falsy
 * @returns The first truthy value, or the second value if first is falsy
 * 
 * @example
 *
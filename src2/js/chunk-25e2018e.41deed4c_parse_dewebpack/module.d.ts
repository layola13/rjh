/**
 * Performs loose equality comparison between two values.
 * Uses the abstract equality comparison algorithm (==) which performs type coercion.
 * 
 * @param left - The left operand to compare
 * @param right - The right operand to compare
 * @returns True if the values are equal after type coercion, false otherwise
 * 
 * @example
 *
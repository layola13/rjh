/**
 * 将任意值转换为对象类型
 * 
 * 此函数确保传入的值被转换为对象形式。
 * 它依赖于一个转换函数（通常用于处理 null/undefined 等边界情况）。
 * 
 * @module ToObject
 * @see {@link https://tc39.es/ecma262/#sec-toobject | ECMAScript ToObject 规范}
 * 
 * @example
 *
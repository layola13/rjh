/**
 * Executes a function and returns whether it threw an error.
 * 
 * @param fn - The function to execute
 * @returns `false` if the function executed successfully, `true` if it threw an error
 * 
 * @example
 *
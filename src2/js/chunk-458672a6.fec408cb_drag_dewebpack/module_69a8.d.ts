/**
 * Checks if an object has a specific property as its own (not inherited).
 * This is a safe wrapper around Object.prototype.hasOwnProperty.
 * 
 * @param obj - The object to check for the property
 * @param propertyKey - The property key to check
 * @returns True if the object has the specified property as its own property, false otherwise
 * 
 * @example
 *
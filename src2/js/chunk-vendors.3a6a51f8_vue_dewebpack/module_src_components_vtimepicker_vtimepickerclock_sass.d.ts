/**
 * VTimePickerClock样式模块类型定义
 * 
 * 该模块为Vuetify时间选择器时钟组件的样式文件。
 * 作为CSS/SASS模块，它不导出任何静态值或类型。
 * 
 * @module VTimePickerClockStyles
 * @packageDocumentation
 */

/**
 * VTimePickerClock样式模块
 * 
 * 这是一个副作用模块，仅用于注入全局样式。
 * 导入此模块会将时钟组件的样式应用到页面中。
 * 
 * @remarks
 * - 无运行时导出
 * - 仅产生副作用（样式注入）
 * - 通常通过Webpack的css-loader/style-loader处理
 * 
 * @example
 *
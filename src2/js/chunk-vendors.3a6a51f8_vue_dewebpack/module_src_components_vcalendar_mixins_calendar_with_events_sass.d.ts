/**
 * VCalendar Calendar With Events Mixin Styles
 * 
 * This module contains the SASS/CSS styles for the VCalendar component's
 * calendar-with-events mixin. It provides styling for calendar components
 * that display events.
 * 
 * @module VCalendarCalendarWithEventsMixin
 * @packageDocumentation
 */

/**
 * Style module for VCalendar's calendar-with-events mixin.
 * This is a side-effect only module that injects styles into the DOM.
 * 
 * @remarks
 * This module has no exports as it only loads CSS/SASS styles.
 * The styles are automatically applied when the module is imported.
 * 
 * @example
 *
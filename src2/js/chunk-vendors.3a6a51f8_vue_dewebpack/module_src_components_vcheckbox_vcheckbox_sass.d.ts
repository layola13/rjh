/**
 * VCheckbox component styles module
 * 
 * This module represents the compiled SASS styles for the VCheckbox component.
 * In a TypeScript build environment, style imports are typically handled by 
 * webpack loaders (e.g., sass-loader, css-loader) and don't export runtime values.
 * 
 * @module VCheckbox.styles
 */

/**
 * Style module for VCheckbox component.
 * When imported, this module applies the VCheckbox styles to the application.
 * 
 * @remarks
 * This is a side-effect only module - it has no exports.
 * The styles are injected into the DOM when the module is imported.
 * 
 * @example
 *
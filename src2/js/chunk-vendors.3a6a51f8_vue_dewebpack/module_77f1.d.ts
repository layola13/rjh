/**
 * 将索引规范化到有效范围内
 * 
 * 此函数处理数组索引，支持负数索引（从末尾计数），
 * 并确保结果索引在 [0, length] 范围内。
 * 
 * @param index - 要规范化的索引值，可以是负数
 * @param length - 数组或集合的长度
 * @returns 规范化后的索引，范围在 [0, length] 之间
 * 
 * @example
 *
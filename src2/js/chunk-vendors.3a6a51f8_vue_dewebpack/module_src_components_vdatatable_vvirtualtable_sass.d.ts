/**
 * VVirtualTable样式模块声明
 * 
 * 该模块导入Vuetify的VDataTable虚拟滚动表格组件的样式文件。
 * Sass/CSS模块在TypeScript中通常没有运行时导出，仅用于副作用（样式注入）。
 * 
 * @module VVirtualTable
 * @packageDocumentation
 */

/**
 * VVirtualTable.sass样式模块
 * 
 * 导入此模块会将VVirtualTable组件的样式注入到应用程序中。
 * 这是一个副作用模块，不导出任何值。
 * 
 * @remarks
 * - 此文件由Webpack处理，在构建时转换为CSS
 * - 通常通过`import './VVirtualTable.sass'`的方式引入
 * - 不包含任何JavaScript/TypeScript运行时代码
 * 
 * @example
 *
/**
 * VBottomSheet component styles module
 * 
 * This module contains the compiled SASS styles for the VBottomSheet component.
 * It's typically imported for side effects only (style injection).
 * 
 * @module VBottomSheet.sass
 */

/**
 * Webpack module loader function for VBottomSheet styles
 * 
 * @param module - The webpack module object containing exports
 * @param exports - The module's exports object
 * @param require - The webpack require function for loading dependencies
 * @returns void - This module has no static exports (side effects only)
 */
declare function loadVBottomSheetStyles(
  module: NodeModule,
  exports: Record<string, never>,
  require: NodeRequire
): void;

export {};
/**
 * Removes leading indentation from a multi-line string while preserving relative indentation.
 * Trims empty lines from the beginning and end of the string.
 * 
 * @param input - The input string to dedent
 * @returns The dedented string with normalized line breaks
 * 
 * @example
 *
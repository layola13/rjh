/**
 * VLayout component module
 * Provides a layout wrapper component based on the grid system
 * @module VLayout
 */

import type { GridComponent } from './grid';

/**
 * Layout component created from the grid factory
 * Used for creating responsive layout containers
 */
declare const VLayout: GridComponent;

export default VLayout;
/**
 * VAvatar 组件样式模块类型定义
 * 
 * @remarks
 * 此模块导出 VAvatar 组件的 Sass 样式。
 * 由于是样式模块，通常通过副作用（side-effect）方式导入，不包含具体的导出内容。
 * 
 * @packageDocumentation
 */

/**
 * VAvatar 样式模块
 * 
 * @remarks
 * 这是一个纯样式模块，导入时会将样式注入到应用中。
 * 通常使用方式：
 *
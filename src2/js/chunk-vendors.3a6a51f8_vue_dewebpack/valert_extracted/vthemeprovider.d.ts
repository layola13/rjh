/**
 * VThemeProvider Module
 * 
 * Provides theme context and styling capabilities for Vue components.
 * This module exports the VThemeProvider component as both named and default export.
 * 
 * @module VThemeProvider
 * @see ./VThemeProvider/VThemeProvider
 */

import VThemeProviderComponent from './VThemeProvider/VThemeProvider';

/**
 * VThemeProvider component for managing application theme.
 * 
 * Wraps child components and provides theme context including:
 * - Color schemes (light/dark mode)
 * - Custom color palettes
 * - Theme variables and CSS custom properties
 * 
 * @example
 *
/**
 * VCalendar Calendar with Events Mixin Styles
 * 
 * This module contains the SASS styles for the VCalendar component's
 * calendar-with-events mixin. As a style-only module, it has no runtime
 * exports and is imported solely for its side effects (CSS injection).
 * 
 * @module components/VCalendar/mixins/calendar-with-events
 * @packageDocumentation
 */

/**
 * Style module for VCalendar calendar-with-events mixin.
 * This is a side-effect only module that injects CSS styles into the document.
 * It contains no runtime exports.
 */
declare module './src/components/VCalendar/mixins/calendar-with-events.sass' {
  // This module has no exports - it's a style-only module
  // Imported for side effects (CSS injection) only
}

export {};
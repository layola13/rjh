/**
 * Selection Controls Styles Module
 * 
 * This module contains styles for selection control components such as:
 * - Checkboxes
 * - Radio buttons
 * - Toggle switches
 * - Other selection UI elements
 * 
 * @module SelectionControlsStyles
 * @remarks This is a style-only module with no runtime exports
 */

/**
 * Selection controls style module type definition
 * 
 * This module imports SASS styles for selection controls and has no runtime exports.
 * It is used for side effects only (injecting CSS into the application).
 */
declare module '@/styles/components/selection-controls' {
  // No exports - style-only module
}

/**
 * Default export type for the selection controls styles module
 * 
 * @remarks Style modules typically don't export any values at runtime.
 * They are imported for their side effects (CSS injection).
 */
export {};
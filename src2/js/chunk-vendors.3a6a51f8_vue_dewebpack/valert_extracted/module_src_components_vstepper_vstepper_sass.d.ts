/**
 * VStepper component styles module
 * 
 * This module contains the SASS/CSS styles for the VStepper component.
 * It's a side-effect import that registers styles globally when imported.
 * 
 * @module VStepper.sass
 * @packageDocumentation
 */

/**
 * VStepper SASS module type definition.
 * 
 * This module has no exports as it only contains CSS/SASS styles that are
 * applied globally when the module is imported. The import is performed
 * purely for its side effects (style registration).
 * 
 * @example
 *
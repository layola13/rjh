/**
 * VRangeSlider SASS module type definitions.
 * 
 * This module imports styles for the VRangeSlider component.
 * SASS/CSS modules in TypeScript typically have no runtime exports,
 * as they are processed by build tools to inject styles into the DOM.
 * 
 * @module VRangeSlider.sass
 */

/**
 * SASS module for VRangeSlider component styling.
 * 
 * When imported, this module applies the component's styles to the application.
 * The actual style injection is handled by the build toolchain (webpack, vite, etc.).
 * 
 * @remarks
 * This is a side-effect import - it has no exported values but applies styles globally
 * or via CSS modules depending on your build configuration.
 * 
 * @example
 *
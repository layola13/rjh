/**
 * VProgressLinear component styles module
 * 
 * This module imports and applies the Sass styles for the VProgressLinear component.
 * It's a side-effect only module that registers styles with the application's style system.
 * 
 * @module VProgressLinear/Styles
 * @packageDocumentation
 */

/**
 * Type definition for the VProgressLinear styles module.
 * 
 * This module has no exports as it only contains side effects (style injection).
 * When imported, it automatically registers the component's styles.
 * 
 * @remarks
 * This is a Sass/CSS module that gets processed by the build system.
 * No runtime exports are provided since styles are applied globally or via CSS modules.
 * 
 * @example
 *
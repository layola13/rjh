/**
 * VCard component stylesheet module
 * 
 * This module imports and applies styles for the VCard component.
 * Since it's a CSS/SASS module with no exports, it only has side effects.
 * 
 * @module VCardStyles
 * @packageDocumentation
 */

/**
 * Side-effect only import that loads VCard component styles.
 * This function is called during module initialization to inject styles into the DOM.
 * 
 * @remarks
 * This is a webpack-processed SASS module with no static exports.
 * The styles are automatically applied when the module is imported.
 * 
 * @internal
 */
export {};
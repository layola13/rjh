/**
 * VPagination component stylesheet module
 * 
 * This module represents the compiled Sass styles for the VPagination component.
 * It contains no runtime exports as it's a side-effect only module that injects
 * CSS styles into the application when imported.
 * 
 * @module VPagination/Styles
 * @packageDocumentation
 */

/**
 * Sass stylesheet module for VPagination component.
 * 
 * This module has no exports - it performs side effects only by injecting
 * compiled CSS styles into the document when loaded by the module system.
 * 
 * @remarks
 * - Automatically imported by the VPagination component
 * - Handles all pagination styling including layout, theming, and responsive design
 * - Compiled from Sass source at build time
 * 
 * @example
 *
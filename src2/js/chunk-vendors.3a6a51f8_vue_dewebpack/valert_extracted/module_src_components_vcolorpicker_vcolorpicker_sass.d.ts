/**
 * VColorPicker component stylesheet module
 * 
 * This module represents the compiled SASS styles for the VColorPicker component.
 * It's a side-effect only module that injects CSS into the application when imported.
 * 
 * @module VColorPicker.styles
 * @packageDocumentation
 */

/**
 * Type definition for the VColorPicker SASS module.
 * 
 * This module contains no exports as it's a CSS-only module that applies
 * styles through side effects when imported. The styles are automatically
 * injected into the DOM by the build system.
 * 
 * @remarks
 * - Import this module for its side effects: `import './VColorPicker.sass'`
 * - No runtime exports are available
 * - Styles are applied globally when the module is loaded
 * 
 * @example
 *
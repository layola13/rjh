/**
 * VDataTableHeader样式模块
 * 
 * 该模块导入VDataTable头部组件的Sass样式文件。
 * 在Webpack构建过程中，样式会被提取并注入到DOM中。
 * 
 * @module VDataTableHeader.sass
 * @packageDocumentation
 */

/**
 * 样式模块导出
 * 
 * 注意：Sass/CSS模块在TypeScript中通常不导出运行时值，
 * 样式通过副作用（side-effect）的方式被加载。
 * 如果使用CSS Modules，可能会导出类名映射对象。
 */
declare const styles: Record<string, never>;

export default styles;
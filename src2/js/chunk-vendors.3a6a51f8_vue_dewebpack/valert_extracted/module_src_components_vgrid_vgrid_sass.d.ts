/**
 * VGrid component styles module
 * 
 * This module exports CSS styles for the VGrid component.
 * Webpack processes this Sass file and injects the styles into the document at runtime.
 * 
 * @module VGridStyles
 * @packageDocumentation
 */

/**
 * Style module for VGrid component
 * 
 * When imported, this module automatically applies the VGrid component styles.
 * No exports are provided as styles are side-effect only (injected into DOM).
 * 
 * @remarks
 * This is a side-effect module that modifies the global CSS scope.
 * The actual style injection is handled by Webpack loaders (e.g., style-loader, css-loader, sass-loader).
 * 
 * @example
 *
/**
 * CSS/SASS module declaration file
 * 
 * This module represents a stylesheet import that doesn't export any JavaScript values.
 * It's used for side effects only (injecting styles into the DOM).
 * 
 * @module styles/main
 */

/**
 * SASS stylesheet module with no exports.
 * 
 * When imported, this module will inject the compiled CSS styles into the document.
 * It doesn't provide any runtime JavaScript exports.
 * 
 * @example
 *
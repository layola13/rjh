/**
 * Pads a number or string with leading zeros (or other characters)
 * @param value - The value to pad
 * @param length - The desired total length after padding (default: 2)
 * @returns The padded string
 */
export default function pad(value: string | number, length?: number): string;
/**
 * VSystemBar component module
 * 
 * This module exports the VSystemBar component, which is typically used to display
 * system-level information bars in Vuetify applications (e.g., status bars, notification bars).
 * 
 * @module VSystemBar
 */

import VSystemBar from './VSystemBar';

/**
 * VSystemBar component
 * 
 * A component for displaying system bars with status information, notifications,
 * or other system-level UI elements.
 * 
 * @public
 */
export { VSystemBar };

/**
 * Default export of VSystemBar component
 * 
 * @public
 * @default
 */
export default VSystemBar;
/**
 * VParallax组件的样式模块类型定义
 * 
 * 此模块导出VParallax组件的CSS样式。
 * 由于是样式文件，它在运行时不导出任何JavaScript值，
 * 仅通过副作用将样式注入到DOM中。
 * 
 * @module VParallax/VParallax.sass
 */

/**
 * 样式模块默认导出
 * 该模块是一个纯CSS/SASS文件，不包含运行时导出
 */
declare const styles: void;

export default styles;
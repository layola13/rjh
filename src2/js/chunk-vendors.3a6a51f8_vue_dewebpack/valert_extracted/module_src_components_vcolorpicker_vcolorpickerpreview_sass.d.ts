/**
 * VColorPicker Preview Component Styles
 * 
 * This module contains the style definitions for the VColorPicker preview component.
 * As a Sass/CSS module, it has side effects (applies styles) but exports no runtime values.
 * 
 * @module VColorPickerPreview
 * @category Components/Styles
 */

/**
 * Style module for VColorPickerPreview component.
 * 
 * This is a side-effect-only module that injects CSS styles into the DOM.
 * It does not export any JavaScript values or types.
 * 
 * @remarks
 * - Imported for side effects only (stylesheet injection)
 * - No runtime exports available
 * - Styles are applied globally or scoped via CSS modules depending on build configuration
 * 
 * @example
 *
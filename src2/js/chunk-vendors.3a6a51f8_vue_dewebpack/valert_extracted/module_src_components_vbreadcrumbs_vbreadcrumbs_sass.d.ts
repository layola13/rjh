/**
 * VBreadcrumbs component styles module
 * 
 * This module contains the compiled SASS styles for the VBreadcrumbs component.
 * It's imported for side effects only (injecting styles into the DOM).
 * 
 * @module VBreadcrumbsStyles
 */

/**
 * Style module for VBreadcrumbs component.
 * Importing this module will inject the breadcrumbs styles into the document.
 * 
 * @remarks
 * This is a side-effect only module with no exports.
 * The styles are automatically applied when the module is imported.
 * 
 * @example
 *
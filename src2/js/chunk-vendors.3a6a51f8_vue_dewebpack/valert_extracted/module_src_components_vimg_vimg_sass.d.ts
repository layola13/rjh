/**
 * VImg component style module
 * 
 * This module contains the compiled CSS styles for the VImg (Vuetify Image) component.
 * It's typically imported for side effects only to inject styles into the application.
 * 
 * @module VImgStyles
 * @packageDocumentation
 */

/**
 * Side-effect import for VImg component styles.
 * 
 * This module doesn't export any runtime values. It's imported solely to ensure
 * that the VImg component's styles are included in the application bundle.
 * 
 * @remarks
 * The original Sass file is compiled and injected into the DOM at runtime by the build system.
 * 
 * @example
 *
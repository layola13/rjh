/**
 * VChip component module
 * 
 * This module exports the VChip component, which is typically used to display
 * compact elements like tags, filters, or status indicators in a UI.
 * 
 * @module VChip
 * @see {@link ./VChip/VChip.ts} for the component implementation
 */

/**
 * VChip component type
 * 
 * A versatile chip component for displaying compact information.
 * Commonly used for tags, labels, filters, and other small UI elements.
 */
export type VChip = unknown;

/**
 * Default export of the VChip component
 * 
 * @default VChip
 */
declare const _default: VChip;
export default _default;

/**
 * Named export for VChip component
 * 
 * @example
 *
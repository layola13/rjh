/**
 * VDialog component styles module
 * 
 * This module contains the Sass stylesheet for the VDialog component.
 * As it's a CSS/Sass module with no exports, it only provides side effects
 * (stylesheet injection) when imported.
 * 
 * @module VDialog.sass
 * @packageDocumentation
 */

/**
 * VDialog Sass module
 * 
 * This module is imported for its side effects only (stylesheet registration).
 * It does not export any values, types, or functions.
 * 
 * @example
 *
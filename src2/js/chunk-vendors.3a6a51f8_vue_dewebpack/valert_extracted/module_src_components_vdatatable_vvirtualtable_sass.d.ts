/**
 * VVirtualTable.sass module declaration
 * 
 * This module represents the compiled CSS/SASS styles for the VVirtualTable component.
 * It contains no runtime exports as it's a style-only module that gets injected into the DOM.
 * 
 * @module VVirtualTable.sass
 * @packageDocumentation
 */

/**
 * Empty module interface for SASS/CSS imports
 * 
 * Style modules in Webpack are typically side-effect imports that inject CSS into the page.
 * They don't export any values, so this declaration ensures TypeScript recognizes the import
 * without expecting any exports.
 * 
 * @example
 *
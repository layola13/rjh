/**
 * VMenu component stylesheet module
 * 
 * This module represents the compiled CSS styles for the VMenu component.
 * It's typically imported for side effects only (applying styles to the component).
 * 
 * @module VMenu.sass
 * @packageDocumentation
 */

/**
 * VMenu stylesheet module interface
 * 
 * This is a CSS module with no exported bindings. The import is used purely
 * for side effects (injecting styles into the DOM at runtime).
 * 
 * @remarks
 * When imported, this module applies the VMenu component styles to the page.
 * It does not export any CSS class names or identifiers.
 * 
 * @example
 *
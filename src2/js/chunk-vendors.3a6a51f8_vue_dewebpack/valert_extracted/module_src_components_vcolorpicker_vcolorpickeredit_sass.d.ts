/**
 * Type definitions for VColorPickerEdit styles module
 * 
 * This module contains the compiled CSS styles for the VColorPicker edit component.
 * It's a side-effect module that injects styles into the DOM when imported.
 * 
 * @module VColorPickerEdit.sass
 * @packageDocumentation
 */

/**
 * Webpack style-loader module that handles CSS injection for VColorPickerEdit component.
 * 
 * This is a side-effect only module - it has no runtime exports.
 * The styles are automatically injected into the document when this module is imported.
 * 
 * @remarks
 * This declaration file represents a SASS/CSS module processed by Webpack.
 * No runtime values are exported; importing this module only applies styles.
 * 
 * @example
 *
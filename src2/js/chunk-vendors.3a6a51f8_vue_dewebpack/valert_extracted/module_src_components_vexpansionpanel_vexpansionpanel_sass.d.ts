/**
 * VExpansionPanel样式模块类型定义
 * 
 * 此模块导入VExpansionPanel组件的样式文件。
 * SASS/CSS模块在TypeScript中通常不导出任何类型化的值。
 * 
 * @module VExpansionPanel.sass
 * @packageDocumentation
 */

/**
 * 样式模块默认导出
 * 
 * SASS/CSS模块的副作用导入，用于将样式注入到文档中。
 * 该模块不包含可导出的JavaScript值，仅用于样式加载。
 * 
 * @remarks
 * 这是一个纯样式模块，通过webpack的css-loader和sass-loader处理。
 * 在运行时，样式会被自动注入到页面的<style>标签中。
 * 
 * @example
 *
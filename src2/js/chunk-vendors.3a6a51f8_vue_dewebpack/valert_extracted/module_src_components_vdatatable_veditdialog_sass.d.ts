/**
 * VEditDialog样式模块类型定义
 * 
 * 该模块为VDataTable组件的编辑对话框样式文件。
 * 由于是Sass样式文件，在TypeScript中导入时不会有运行时导出，
 * 仅用于副作用（注入样式到DOM）。
 * 
 * @module VEditDialog.sass
 * @packageDocumentation
 */

/**
 * VEditDialog样式模块
 * 
 * 这是一个纯样式模块，导入后会将编辑对话框的样式注入到应用中。
 * 该模块没有任何导出值，仅执行副作用操作。
 * 
 * @example
 *
/**
 * VRadioGroup component styles module
 * 
 * This module contains the compiled CSS styles for the VRadioGroup component.
 * Imported for side effects only (style injection into the DOM).
 * 
 * @module VRadioGroup/Styles
 * @packageDocumentation
 */

/**
 * Type definition for the VRadioGroup stylesheet module.
 * 
 * This module exports no runtime values - it only injects styles into the document.
 * The import is used for its side effects (CSS injection via webpack style-loader).
 * 
 * @remarks
 * This is a CSS/SASS module processed by webpack loaders. When imported,
 * it automatically applies the VRadioGroup component styles to the page.
 * 
 * @example
 *
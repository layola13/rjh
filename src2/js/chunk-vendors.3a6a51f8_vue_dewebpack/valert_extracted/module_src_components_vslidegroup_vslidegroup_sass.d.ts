/**
 * VSlideGroup component styles module
 * 
 * This module imports and applies SASS styles for the VSlideGroup component.
 * It's typically used internally by the VSlideGroup component to inject its styles.
 * 
 * @module VSlideGroupStyles
 * @packageDocumentation
 */

/**
 * Type definition for the VSlideGroup SASS module
 * 
 * This module has no static exports as it only registers styles globally
 * when imported. The styles are applied via side effects during module evaluation.
 * 
 * @remarks
 * - This is a side-effect-only module that injects CSS into the document
 * - No exports are available from this module
 * - Importing this module will automatically apply VSlideGroup styles
 * 
 * @example
 *
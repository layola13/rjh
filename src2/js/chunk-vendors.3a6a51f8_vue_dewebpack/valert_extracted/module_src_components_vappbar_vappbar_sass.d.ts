/**
 * VAppBar component styles module
 * 
 * This module contains the compiled SASS styles for the VAppBar component.
 * It's typically imported for side effects only (style injection into the DOM).
 * 
 * @module VAppBar/VAppBar.sass
 * @packageDocumentation
 */

/**
 * Style module for VAppBar component.
 * 
 * This is a side-effect module that injects CSS styles when imported.
 * It does not export any runtime values or types.
 * 
 * @remarks
 * In a properly configured TypeScript project with style loaders,
 * this module is imported solely for its side effects (CSS injection).
 * 
 * @example
 *
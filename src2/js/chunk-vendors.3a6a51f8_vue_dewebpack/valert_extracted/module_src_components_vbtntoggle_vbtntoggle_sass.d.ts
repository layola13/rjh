/**
 * VBtnToggle Component Styles
 * 
 * This module contains the compiled CSS styles for the VBtnToggle component.
 * It's a style-only module that doesn't export any runtime values.
 * 
 * @module VBtnToggle.sass
 * @packageDocumentation
 */

/**
 * Style module for VBtnToggle component.
 * This is a side-effect import that injects styles into the document.
 * No exports are provided as this is a pure CSS/SASS module.
 */
declare module './components/VBtnToggle/VBtnToggle.sass' {
  /**
   * This module has no exports.
   * Importing it will apply the VBtnToggle styles to your application.
   * 
   * @example
   *
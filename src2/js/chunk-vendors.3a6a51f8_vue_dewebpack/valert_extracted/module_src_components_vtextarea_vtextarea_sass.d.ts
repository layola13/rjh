/**
 * VTextarea component styles module
 * 
 * This module contains the compiled Sass styles for the VTextarea component.
 * It's typically imported for side effects only (style injection).
 * 
 * @module VTextarea/Styles
 * @packageDocumentation
 */

/**
 * Style module for VTextarea component.
 * This is a side-effect only module that injects CSS into the document.
 * 
 * @remarks
 * When imported, this module automatically applies the VTextarea component styles
 * to the DOM. It exports no runtime values.
 * 
 * @example
 *
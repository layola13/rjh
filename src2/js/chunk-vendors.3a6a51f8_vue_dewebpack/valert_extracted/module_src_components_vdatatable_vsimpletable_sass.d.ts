/**
 * VSimpleTable Sass 样式模块类型定义
 * 
 * 该模块包含 VSimpleTable 组件的样式导入，通常由 Webpack 的 CSS/Sass loader 处理。
 * 在运行时，样式会被注入到文档中，该模块本身不导出任何值。
 * 
 * @module VSimpleTable.sass
 * @packageDocumentation
 */

/**
 * Sass 样式模块
 * 
 * 此模块仅用于副作用（样式注入），不包含任何导出。
 * 当被导入时，相关的 CSS 样式会自动应用到页面中。
 * 
 * @example
 *
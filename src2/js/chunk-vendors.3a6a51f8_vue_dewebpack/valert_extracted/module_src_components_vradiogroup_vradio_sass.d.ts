/**
 * VRadio component styles module
 * 
 * This module contains the Sass/CSS styles for the VRadio component,
 * which is part of the VRadioGroup component family.
 * 
 * @module VRadio.sass
 * @packageDocumentation
 */

/**
 * Style module for VRadio component.
 * 
 * When imported, this module applies the necessary CSS styles for the radio button component.
 * It has no exports as it's a pure CSS/Sass module that only produces side effects (style injection).
 * 
 * @remarks
 * This is a side-effect only module - importing it will inject styles into the document.
 * No runtime values or functions are exported.
 * 
 * @example
 *
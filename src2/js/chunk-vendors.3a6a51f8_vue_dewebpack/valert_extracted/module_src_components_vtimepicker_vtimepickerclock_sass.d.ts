/**
 * VTimePickerClock styles module
 * 
 * This module contains the compiled CSS styles for the VTimePickerClock component.
 * It's a side-effect import that injects styles into the document when loaded.
 * 
 * @module VTimePickerClockStyles
 * @package Vuetify
 */

/**
 * Style module with no exports.
 * When imported, this module applies the VTimePickerClock component styles to the DOM.
 * 
 * @example
 *
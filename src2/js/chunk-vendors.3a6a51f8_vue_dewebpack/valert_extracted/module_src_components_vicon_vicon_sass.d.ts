/**
 * VIcon component style module
 * 
 * This module contains the compiled CSS styles for the VIcon component.
 * It's a side-effect only module that injects styles into the DOM when imported.
 * 
 * @module VIcon/VIcon.sass
 * @packageDocumentation
 */

/**
 * Style injection function that applies VIcon component styles to the document.
 * This function is typically called automatically during module initialization.
 * 
 * Note: This is a style-only module with no runtime exports.
 * The function signature exists for webpack's style-loader compatibility.
 * 
 * @param module - Webpack module object (unused in type definitions)
 * @param exports - Module exports object (unused in type definitions)
 * @param require - Webpack require function (unused in type definitions)
 * @returns void - No return value, side-effect only
 * 
 * @internal
 */
declare function styleInjector(
  module: unknown,
  exports: unknown,
  require: unknown
): void;

export {};
/**
 * VEditDialog样式模块类型定义
 * 
 * 该模块导入VDataTable组件的VEditDialog子组件的SASS样式文件。
 * 由于是样式模块，不导出任何JavaScript运行时值。
 * 
 * @module VEditDialog.sass
 * @packageDocumentation
 */

/**
 * SASS样式模块
 * 
 * 此模块仅用于副作用导入（side-effect import），
 * 会将编译后的CSS样式注入到DOM中，但不提供任何可用的导出成员。
 * 
 * @remarks
 * 这是一个纯样式模块，通常在组件文件中通过 `import './VEditDialog.sass'` 导入
 * 
 * @example
 *
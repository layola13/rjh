/**
 * VProgressLinear component styles module
 * 
 * This module exports the compiled SASS styles for the VProgressLinear component.
 * It's a side-effect only module that injects CSS into the document when imported.
 * 
 * @module VProgressLinearStyles
 * @packageDocumentation
 */

/**
 * Side-effect function that loads and applies VProgressLinear component styles.
 * This function is called automatically when the module is imported.
 * 
 * @remarks
 * This is a webpack-generated module from the original SASS file:
 * `./src/components/VProgressLinear/VProgressLinear.sass`
 * 
 * The module has no static exports - it only registers CSS styles globally.
 * 
 * @param e - Module exports object (unused)
 * @param t - Module metadata (unused)
 * @param n - Webpack require function (unused)
 * 
 * @returns void - This function performs side effects only
 */
declare function loadVProgressLinearStyles(
  e: Record<string, never>,
  t: unknown,
  n: (moduleId: string) => unknown
): void;

/**
 * Export declaration for the side-effect module.
 * Importing this module will inject VProgressLinear styles into the DOM.
 * 
 * @example
 *
/**
 * VListItemGroup样式模块
 * 
 * 该模块导出VListItemGroup组件的Sass样式。
 * 由于Sass/CSS模块在运行时不导出值，此文件仅包含副作用（样式注入）。
 * 
 * @module VListItemGroup.sass
 * @packageDocumentation
 */

/**
 * 样式模块副作用声明
 * 
 * 此模块在导入时会将VListItemGroup的样式注入到DOM中，
 * 但不导出任何JavaScript值或类型。
 * 
 * @remarks
 * 通常通过 `import './VListItemGroup.sass'` 或 
 * `import 'vuetify/src/components/VList/VListItemGroup.sass'` 使用
 * 
 * @example
 *
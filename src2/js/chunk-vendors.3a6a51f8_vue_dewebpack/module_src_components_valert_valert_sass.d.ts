/**
 * VAlert Component Styles Module
 * 
 * This module imports and applies styles for the VAlert component.
 * It's a side-effect-only module that registers CSS styles without exporting any values.
 * 
 * @module VAlert/Styles
 * @packageDocumentation
 */

/**
 * Style imports for the VAlert component.
 * This is a Sass stylesheet module that applies visual styling to VAlert instances.
 * 
 * @remarks
 * This module has no runtime exports - it exists solely to inject CSS into the application.
 * The styles are typically processed by a CSS loader (e.g., sass-loader, css-loader) 
 * during the build process.
 * 
 * @example
 *
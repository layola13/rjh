/**
 * Creates a simple functional component for breadcrumb dividers.
 * This component renders a list item element with the class "v-breadcrumbs__divider".
 */

import { createSimpleFunctional } from '../../util/helpers';

/**
 * VBreadcrumbsDivider component
 * A functional component that renders a divider between breadcrumb items.
 * 
 * @remarks
 * This is a simple functional component that wraps content in a <li> element
 * with the "v-breadcrumbs__divider" CSS class.
 * 
 * @example
 *
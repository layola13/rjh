/**
 * VSkeletonLoader component styles module.
 * 
 * This module contains the compiled SASS styles for the VSkeletonLoader component.
 * It's typically imported for side effects only (injecting styles into the DOM).
 * 
 * @module VSkeletonLoaderStyles
 * @packageDocumentation
 */

/**
 * Style injection function that registers VSkeletonLoader styles.
 * This function is called automatically when the module is imported.
 * 
 * @remarks
 * In Webpack-based builds, this function handles CSS injection into the document.
 * The module has no static exports as it only provides side effects (style registration).
 * 
 * @internal
 */
declare function injectStyles(): void;

export {};
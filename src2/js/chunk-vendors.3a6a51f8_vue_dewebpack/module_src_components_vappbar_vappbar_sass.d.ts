/**
 * VAppBar Component Styles Module
 * 
 * This module exports the compiled styles for the VAppBar component.
 * It is typically imported for side effects only (style injection).
 * 
 * @module VAppBar/Styles
 * @packageDocumentation
 */

/**
 * Style module for VAppBar component.
 * Importing this module will inject the VAppBar styles into the document.
 * 
 * @remarks
 * This is a CSS/SASS module that has been processed by webpack.
 * It has no runtime exports and is imported purely for its side effects.
 * 
 * @example
 *
/**
 * VDialog component styles module
 * 
 * This module contains the Sass/CSS styles for the VDialog component.
 * In Webpack builds, style modules typically don't export runtime values
 * but are processed by style-loader/css-loader to inject styles into the DOM.
 * 
 * @module VDialog/VDialog.sass
 */

/**
 * Style module for VDialog component.
 * 
 * @remarks
 * This is a side-effect only module that injects CSS styles when imported.
 * It does not export any TypeScript values or types at runtime.
 * 
 * The module is processed by Webpack loaders (typically sass-loader → css-loader → style-loader)
 * to transform SASS into CSS and inject it into the document's <head>.
 * 
 * @example
 *
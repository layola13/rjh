/**
 * VPagination组件样式模块
 * 
 * 该模块包含VPagination（分页）组件的样式定义。
 * 这是一个样式副作用模块，导入时会自动注入CSS样式。
 * 
 * @module VPaginationStyles
 * @packageDocumentation
 */

/**
 * 样式模块类型定义
 * 
 * SASS/CSS模块通常没有导出值，仅用于副作用（注入样式）。
 * 导入此模块会将VPagination组件的样式添加到文档中。
 * 
 * @example
 *
/**
 * VDataTable样式模块类型定义
 * 
 * 该模块导出VDataTable组件的SASS样式。
 * 由于是样式文件，在TypeScript中通常没有运行时导出值。
 * 
 * @module VDataTable.sass
 * @packageDocumentation
 */

/**
 * SASS样式模块
 * 
 * 此模块包含VDataTable组件的所有样式定义。
 * 导入此模块会将样式注入到应用程序中，但不提供任何JavaScript API。
 * 
 * @example
 *
/**
 * VFooter Component Styles Module
 * 
 * This module exports the compiled CSS styles for the VFooter component.
 * Imported from: ./src/components/VFooter/VFooter.sass
 * 
 * @module VFooter.styles
 * @remarks
 * This is a side-effect import module that injects styles into the DOM.
 * No runtime exports are provided as the styles are applied globally.
 */

/**
 * Style injection function
 * 
 * This function is typically called by the module loader to inject
 * the compiled SASS/CSS styles into the document head.
 * 
 * @param exports - The module exports object (unused in style modules)
 * @param module - The module metadata object (unused in style modules)
 * @param require - The module require function (unused in this case)
 * @returns void - This module has side effects only and returns nothing
 */
declare function injectStyles(
  exports: Record<string, never>,
  module: unknown,
  require: unknown
): void;

export {};
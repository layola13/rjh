/**
 * VRadioGroup component styles module
 * 
 * This module imports the SASS styles for the VRadioGroup component.
 * It has no runtime exports as it only contains CSS/SASS declarations.
 * 
 * @module VRadioGroup/Styles
 * @packageDocumentation
 */

/**
 * Style imports for VRadioGroup component.
 * This file is processed by webpack's style-loader and has no JavaScript exports.
 * 
 * The actual styles are injected into the DOM at runtime by webpack's style handling.
 */
declare module './components/VRadioGroup/VRadioGroup.sass' {
  /**
   * No exports - this module only contains CSS styles that are
   * automatically injected into the document when imported.
   */
  const styles: void;
  export default styles;
}
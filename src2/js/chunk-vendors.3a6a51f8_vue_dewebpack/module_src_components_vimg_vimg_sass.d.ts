/**
 * VImg Component Styles Module
 * 
 * This module contains the style definitions for the VImg (Vuetify Image) component.
 * It's a CSS/SASS module that doesn't export any runtime JavaScript values.
 * 
 * @module VImgStyles
 * @packageDocumentation
 */

/**
 * Style module for VImg component.
 * 
 * This is a side-effect only module that injects styles into the document.
 * It does not provide any programmatic exports or runtime functionality.
 * 
 * @remarks
 * This module is typically imported for its side effects only:
 *
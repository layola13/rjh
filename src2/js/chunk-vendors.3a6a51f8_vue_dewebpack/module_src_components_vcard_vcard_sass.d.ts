/**
 * VCard component stylesheet module
 * 
 * This module represents the compiled SASS styles for the VCard component.
 * It has no runtime exports as it's a side-effect import that injects CSS into the DOM.
 * 
 * @module VCard/Styles
 * @packageDocumentation
 */

/**
 * VCard SASS module
 * 
 * This is a CSS-only module with no JavaScript exports.
 * When imported, it registers the VCard component styles with the application.
 * 
 * @remarks
 * This module is typically imported for its side effects only:
 *
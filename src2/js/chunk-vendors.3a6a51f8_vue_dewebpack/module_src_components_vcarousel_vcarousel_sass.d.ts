/**
 * VCarousel component stylesheet module
 * 
 * This module represents the compiled SASS styles for the VCarousel component.
 * It contains no runtime exports as it's a side-effect-only CSS import.
 * 
 * @module VCarousel/Styles
 * @packageDocumentation
 */

/**
 * Type definition for the VCarousel SASS module.
 * 
 * This module has no exports - it only provides side effects (CSS injection).
 * When imported, it automatically injects the VCarousel styles into the DOM.
 * 
 * @remarks
 * This is a CSS/SASS module with no JavaScript exports.
 * The module exists solely for its side effects (style injection).
 * 
 * @example
 *
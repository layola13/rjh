/**
 * VFlex Component
 * 
 * A flexible grid component that provides flex layout capabilities.
 * Built on top of the base grid system.
 * 
 * @module VFlex
 * @see {@link grid} - Base grid factory function
 */

/**
 * Creates a flex-based grid component.
 * 
 * This component extends the base grid system with flex-specific layout properties,
 * allowing for flexible and responsive grid layouts.
 * 
 * @returns A Vue component configured for flex layout
 * 
 * @example
 *
/**
 * VThemeProvider Module
 * 
 * Provides theme management and context for the application.
 * This module serves as the main entry point for VThemeProvider functionality.
 * 
 * @module VThemeProvider
 * @see ./VThemeProvider/VThemeProvider for implementation details
 */

/**
 * VThemeProvider component or class.
 * 
 * This is the primary export that handles theme provision throughout the component tree.
 * It typically wraps the application or sections of it to provide consistent theming.
 * 
 * @remarks
 * Imported from './VThemeProvider/VThemeProvider' implementation file.
 */
export { default as VThemeProvider } from './VThemeProvider/VThemeProvider';

/**
 * Default export of VThemeProvider.
 * 
 * Allows importing the VThemeProvider using default import syntax:
 *
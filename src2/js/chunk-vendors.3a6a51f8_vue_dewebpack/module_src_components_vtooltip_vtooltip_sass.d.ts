/**
 * VTooltip component styles module
 * 
 * This module exports the compiled styles for the VTooltip component.
 * It's typically imported for side effects only (applying CSS to the DOM).
 * 
 * @module VTooltip.sass
 * @packageDocumentation
 */

/**
 * Side-effect import that injects VTooltip styles into the DOM.
 * This module has no runtime exports.
 * 
 * @remarks
 * - Automatically handled by webpack style-loader or similar CSS injection mechanism
 * - No need to access module contents directly
 * 
 * @example
 *
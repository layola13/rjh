/**
 * Creates an iterator result object compatible with the Iterator protocol.
 * 
 * @template T - The type of the iterator value
 * @param isDone - Whether the iteration is complete
 * @param value - The current value yielded by the iterator
 * @returns An IteratorResult object with the value and done status
 * 
 * @example
 *
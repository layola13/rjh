/**
 * VSheet Component Styles
 * 
 * Type definitions for VSheet.sass style module.
 * This module contains CSS styles for the VSheet component.
 * 
 * @module components/VSheet/VSheet.sass
 */

/**
 * Style module for VSheet component.
 * 
 * Since this is a CSS/SASS module without static exports,
 * it typically side-effects the global styles or is imported
 * for CSS Modules class name mapping.
 * 
 * @remarks
 * This module has no runtime exports - it's imported for side effects only.
 * The styles are automatically injected into the document when imported.
 * 
 * @example
 *
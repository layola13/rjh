/**
 * Extends an object with properties from another object.
 * 
 * @param target - The target object to extend
 * @param source - The source object containing properties to copy
 * @param override - If true, override existing properties; if false, only add non-existing properties
 * @returns The extended target object
 * 
 * @example
 *
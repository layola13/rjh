/**
 * VBtn component styles module
 * 
 * This module imports and applies the styles for the VBtn (Vuetify Button) component.
 * It's a side-effect only module that registers CSS styles without exporting any values.
 * 
 * @module VBtn/Styles
 * @packageDocumentation
 */

/**
 * Style module for VBtn component.
 * 
 * This is a CSS/SASS module import that has no runtime exports.
 * The styles are applied globally when this module is imported.
 * 
 * @remarks
 * This declaration represents a Webpack-processed SASS file that:
 * - Contains button component styling
 * - Has no static exports (side-effect only)
 * - Registers styles in the global scope
 * 
 * @example
 *
/**
 * Calculates the new year-month string after adding/subtracting months.
 * Handles year boundaries correctly (e.g., going from December to January or vice versa).
 * 
 * @param dateString - Date string in "YYYY-MM" format (e.g., "2023-12")
 * @param monthOffset - Number of months to add (positive) or subtract (negative)
 * @returns New date string in "YYYY-MM" format after applying the month offset
 * 
 * @example
 *
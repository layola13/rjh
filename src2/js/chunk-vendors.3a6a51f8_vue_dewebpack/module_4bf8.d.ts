/**
 * Converts a value to an object by wrapping it with Object() constructor.
 * This ensures that primitive values are converted to their object wrappers,
 * while already existing objects are returned as-is.
 * 
 * @param value - The value to be converted to an object. Can be any type including primitives.
 * @returns An object representation of the input value.
 * 
 * @example
 *
/**
 * VHover component module
 * 
 * This module provides the VHover component which handles hover interactions.
 * The component is imported from './VHover.ts' and re-exported as both a named
 * and default export for flexible import options.
 * 
 * @module components/VHover
 */

/**
 * VHover component class or interface
 * 
 * A component that manages hover state and interactions.
 * This can be used to detect and respond to hover events on elements.
 */
export declare const VHover: VHoverComponent;

/**
 * Default export of the VHover component
 * 
 * @default VHover
 */
declare const _default: VHoverComponent;
export default _default;

/**
 * VHover component type definition
 * 
 * Represents the VHover component interface. The actual implementation
 * details would depend on the framework being used (React, Vue, etc.)
 */
interface VHoverComponent {
  // Component implementation details would be defined here
  // based on the actual VHover.ts file structure
}
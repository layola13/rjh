/**
 * VRating component Sass module
 * 
 * This module represents the compiled styles for the VRating component.
 * It's a side-effect only module that injects CSS into the application.
 * 
 * @module VRating/Styles
 * @packageDocumentation
 */

/**
 * Sass style module for VRating component.
 * 
 * This module has no exports as it only contains side effects (CSS injection).
 * When imported, it automatically applies the VRating component styles to the DOM.
 * 
 * @remarks
 * This is a compiled Sass module that gets processed by Webpack's style-loader
 * or similar CSS loading mechanism. It contains no runtime exports.
 * 
 * @example
 *
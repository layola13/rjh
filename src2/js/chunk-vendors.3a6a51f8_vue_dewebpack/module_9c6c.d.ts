/**
 * Adds a property to the Array.prototype[Symbol.unscopables] object.
 * 
 * This module extends the unscopables registry of Array.prototype, which controls
 * what properties are excluded from `with` statement bindings in JavaScript.
 * 
 * @module ArrayUnscopablesRegistry
 * 
 * @example
 *
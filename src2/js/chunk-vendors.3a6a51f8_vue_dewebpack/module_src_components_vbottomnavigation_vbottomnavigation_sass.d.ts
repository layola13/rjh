/**
 * VBottomNavigation component stylesheet module
 * 
 * This module represents the compiled SASS styles for the VBottomNavigation component.
 * In production builds, CSS/SASS modules are typically extracted and don't export runtime values.
 * 
 * @module VBottomNavigation.sass
 * @packageDocumentation
 */

/**
 * Type definition for the VBottomNavigation SASS module.
 * 
 * Since this is a style-only module with no static exports (side-effect import),
 * it only provides ambient type information and doesn't export any runtime values.
 * 
 * @remarks
 * This module is imported for its side effects (injecting styles into the DOM).
 * No values should be imported from this module.
 * 
 * @example
 *
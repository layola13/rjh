/**
 * VOverlay component styles module
 * 
 * This module contains the compiled SASS styles for the VOverlay component.
 * It's a side-effect only module that registers styles when imported.
 * 
 * @module VOverlay.sass
 * @packageDocumentation
 */

/**
 * Style module with no exports - imports for side effects only.
 * When this module is imported, it injects the VOverlay component styles into the DOM.
 * 
 * @example
 *
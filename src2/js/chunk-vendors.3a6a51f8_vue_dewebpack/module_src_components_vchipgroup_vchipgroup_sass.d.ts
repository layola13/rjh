/**
 * VChipGroup样式模块声明
 * 
 * 该模块导出VChipGroup组件的SASS样式。
 * 在Webpack构建过程中，SASS文件被编译并注入到应用中。
 * 
 * @module VChipGroup/VChipGroup
 * @packageDocumentation
 */

/**
 * VChipGroup样式模块
 * 
 * 此模块不包含任何运行时导出，样式通过副作用(side-effect)的方式
 * 自动注入到DOM中。TypeScript应将此模块视为纯样式导入。
 * 
 * @example
 *
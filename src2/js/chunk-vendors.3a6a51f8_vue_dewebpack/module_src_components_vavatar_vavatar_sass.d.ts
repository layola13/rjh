/**
 * VAvatar component styles module
 * 
 * This is a CSS/SASS module that doesn't export any runtime values.
 * It's imported for side effects only (style injection).
 * 
 * @module VAvatar.sass
 * @packageDocumentation
 */

/**
 * VAvatar SASS module with no runtime exports.
 * 
 * @remarks
 * This module contains only stylesheet definitions that are injected
 * into the document at runtime. It has no JavaScript/TypeScript exports.
 * 
 * Typical usage:
 *
/**
 * VProgressLinear Component Module
 * 
 * Provides a linear progress indicator component for displaying
 * progress states, loading operations, or determinate/indeterminate progress.
 * 
 * @module VProgressLinear
 */

import VProgressLinearComponent from './VProgressLinear';

/**
 * Linear progress bar component
 * 
 * @remarks
 * Typically used to visualize loading states, file uploads, or any
 * operation with measurable progress. Supports both determinate
 * (with value) and indeterminate (animated) modes.
 * 
 * @example
 *
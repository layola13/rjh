/**
 * VVirtualScroll component styles module
 * 
 * This module contains the Sass/CSS styles for the VVirtualScroll component.
 * It's a side-effect import that registers styles globally when imported.
 * 
 * @module VVirtualScroll.sass
 * @packageDocumentation
 */

/**
 * Webpack module loader function for VVirtualScroll styles.
 * This is a CSS/Sass module with no exports - it only applies styles as side effects.
 * 
 * @remarks
 * This declaration file represents a Sass stylesheet that gets processed by Webpack.
 * The module has no static exports and is imported solely for its side effects
 * (applying CSS styles to the DOM).
 * 
 * @example
 *
/**
 * VCalendarDaily component styles module.
 * 
 * This module contains the compiled CSS styles for the VCalendarDaily component.
 * It's a side-effect only module that injects styles into the DOM when imported.
 * 
 * @module VCalendarDaily.styles
 * @packageDocumentation
 */

/**
 * Style injection module for VCalendarDaily component.
 * 
 * This is a Webpack-processed SASS module that has no runtime exports.
 * The styles are automatically injected into the document when this module is imported.
 * 
 * @remarks
 * This module should be imported for its side effects only:
 *
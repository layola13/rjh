/**
 * Main stylesheet module
 * 
 * This module represents the compiled SASS stylesheet for the application.
 * It's imported for side effects only (CSS injection) and exports no values.
 * 
 * @module styles/main
 */

/**
 * Side-effect only module that injects the main application styles.
 * No exports are provided as this is a CSS module processed by webpack loaders.
 * 
 * @remarks
 * This declaration file represents a SASS/CSS module that is imported for its side effects.
 * When imported, it will inject the compiled styles into the DOM.
 * 
 * @example
 *
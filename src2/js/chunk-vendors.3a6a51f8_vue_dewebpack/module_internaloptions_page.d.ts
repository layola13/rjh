/**
 * Internal options page update handler
 * Emits an update event for the page property in a Vue component
 * 
 * @module module_internalOptions_page
 * @remarks This is typically used in Vue components with v-model:page binding
 */

/**
 * Updates the page value by emitting an update event
 * 
 * @param newPage - The new page number or page data to emit
 * @returns void
 * 
 * @example
 *
/**
 * VLazy component module
 * 
 * Provides lazy loading functionality for components or content.
 * This module re-exports the VLazy component as both named and default export.
 * 
 * @module VLazy
 * @see {@link ./VLazy/VLazy.ts} for the actual implementation
 */

import VLazyComponent from './VLazy/VLazy';

/**
 * VLazy component for lazy loading content
 * 
 * Enables deferred loading of components or content until they are needed,
 * improving initial page load performance.
 * 
 * @example
 *
/**
 * ES Module marker for TypeScript declaration file.
 * This module was originally part of a Webpack bundle (module ID: 380).
 * It served to mark the exports object as an ES module by setting the __esModule flag.
 */

/**
 * Type definition for ES module marker.
 * When transpiled to CommonJS, TypeScript/Babel adds __esModule: true
 * to indicate the module was originally an ES module.
 */
export {};

/**
 * Alternative representation if this was intended as an ambient module:
 * declare module 'module_380' {
 *   // Empty module that only sets __esModule flag
 * }
 */
/**
 * Combines multiple observables into a single observable that emits values from all source observables.
 * 
 * @module combineLatest
 * @description
 * This module provides functionality to combine multiple observable sources.
 * It supports both array and individual observable arguments, with optional scheduler.
 * 
 * @example
 *
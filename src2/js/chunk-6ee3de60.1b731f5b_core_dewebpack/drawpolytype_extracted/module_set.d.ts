/**
 * Sets the polyline identifier for the current sprite line.
 * 
 * @param polyId - The unique identifier to assign to the polyline
 */
function setPolyId(polyId: string | number): void {
  this.spLine.polyId = polyId;
}
/**
 * TypeScript module declaration file
 * This module appears to be a simple ES module marker that sets the __esModule property.
 * In modern TypeScript, this is handled automatically by the compiler.
 */

/**
 * Re-exported module namespace.
 * This declaration preserves the original module's ES module semantics.
 * 
 * @remarks
 * The original webpack module only set the __esModule flag to true,
 * which indicates this was compiled from an ES module source.
 * In TypeScript, this is implicit when using ES module syntax.
 */
declare module 'module_382' {
  /**
   * This module exports nothing explicitly.
   * The original code only marked itself as an ES module.
   */
}

export {};
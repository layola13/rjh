/**
 * Provides access to the native propertyIsEnumerable method.
 * This utility exposes the Object.prototype.propertyIsEnumerable function
 * for checking if a specified property is enumerable.
 * 
 * @module PropertyIsEnumerable
 */

/**
 * Reference to the native propertyIsEnumerable method from Object.prototype.
 * Used to determine whether a specified property is enumerable.
 * 
 * @function
 * @param {PropertyKey} property - The name of the property to test
 * @returns {boolean} true if the property is enumerable, false otherwise
 * 
 * @example
 *
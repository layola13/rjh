/**
 * Webpack asset module export
 * Exports the public URL path for the CrossHandle image asset
 * 
 * @module ImageAssetExport
 * @description This module provides the resolved public path for a static image asset (CrossHandle.png)
 * that was processed by Webpack's asset/resource loader during the build process.
 */

/**
 * The public URL path to the CrossHandle image asset
 * Format: `${publicPath}img/CrossHandle.cadf733b.png`
 * where publicPath is the Webpack configuration's output.publicPath
 * 
 * @example
 *
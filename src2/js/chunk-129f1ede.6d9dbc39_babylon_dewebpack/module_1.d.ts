/**
 * Validates that a number is greater than zero
 * 
 * @param value - The number to validate
 * @returns `true` if the value is greater than 0, otherwise returns the error message "must >0"
 * 
 * @example
 *
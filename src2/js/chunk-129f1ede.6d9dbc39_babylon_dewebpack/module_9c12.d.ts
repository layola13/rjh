/**
 * 判断一个值是否为安全整数
 * 
 * 该函数检查给定值是否满足以下条件：
 * 1. 不是对象类型（通过 isObject 检查）
 * 2. 是有限数值（通过 isFinite 检查）
 * 3. 等于其向下取整后的值（即没有小数部分）
 * 
 * @param value - 需要检查的值
 * @returns 如果值是安全整数则返回 true，否则返回 false
 * 
 * @example
 *
/**
 * Checks if the right card component has unsaved changes.
 * 
 * @returns {boolean} True if the right card has been modified and not saved, false otherwise
 */
function isRightCardDirty(): boolean;
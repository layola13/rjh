/**
 * 检查给定值是否为有效的数组索引
 * 
 * 有效的数组索引必须满足以下条件：
 * 1. 不是 null 或 undefined
 * 2. 是一个非负整数或可以转换为非负整数的值
 * 3. 在有效的数组索引范围内（0 到 2^32 - 1）
 * 
 * @param value - 要检查的值，可以是任意类型
 * @returns 如果是有效的数组索引返回 true，否则返回 false
 * 
 * @example
 *
/**
 * Base64-encoded PNG image data URL module
 * 
 * This module exports a data URL containing a PNG image encoded in base64 format.
 * The image dimensions are 317x317 pixels (as indicated by the metadata).
 * 
 * Original Module ID: 9421
 * 
 * @module ImageDataURL
 */

/**
 * A data URL string containing a base64-encoded PNG image.
 * 
 * Format: `data:image/png;base64,{base64EncodedData}`
 * 
 * This string can be used directly as the `src` attribute of an HTML `<img>` element
 * or in CSS as a background image.
 * 
 * @example
 *
/**
 * Creates an Observable that immediately emits a single value and completes.
 * This is a simplified implementation of RxJS's `of()` operator for single values.
 * 
 * @template T - The type of the value to be emitted
 * @param value - The value to emit immediately
 * @returns An Observable that emits the provided value and then completes
 * 
 * @example
 *
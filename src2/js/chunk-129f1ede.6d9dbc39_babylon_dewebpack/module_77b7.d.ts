/**
 * Module entry point that imports stylesheet dependencies.
 * This module serves as a side-effect import to ensure CSS/SCSS files are loaded.
 * 
 * @module module_77b7
 * @remarks
 * This is a webpack module that was originally used to import styles.
 * The module ID "7edf" likely refers to a CSS/SCSS file that should be loaded
 * as a side effect when this module is imported.
 */

/**
 * Imports the stylesheet module with ID "7edf".
 * This is a side-effect only import that ensures styles are bundled and loaded.
 */
import "7edf";

export {};
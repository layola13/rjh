/**
 * Validates that a value is present (truthy).
 * 
 * This is a common validation function used in form validation or data validation contexts.
 * Returns `true` if the value is truthy, otherwise returns the string "required" as an error message.
 * 
 * @param value - The value to validate
 * @returns `true` if the value is truthy, otherwise returns the error message "required"
 * 
 * @example
 *
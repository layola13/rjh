/**
 * Babylon.js Procedural Textures Library
 * 
 * This module provides a collection of procedurally generated textures
 * for creating dynamic materials without external image files.
 * 
 * @packageDocumentation
 */

/**
 * Procedural texture that generates a brick wall pattern.
 * 
 * Creates a realistic brick wall texture with customizable brick size,
 * mortar color, and brick color variations.
 */
export class BrickProceduralTexture {
  /**
   * Creates a new brick procedural texture.
   * 
   * @param name - The name of the texture
   * @param size - The size of the texture (width and height)
   * @param scene - The scene the texture belongs to
   */
  constructor(name: string, size: number, scene: any);
}

/**
 * Procedural texture that generates animated cloud patterns.
 * 
 * Creates volumetric cloud-like effects with customizable colors
 * and animation speed.
 */
export class CloudProceduralTexture {
  /**
   * Creates a new cloud procedural texture.
   * 
   * @param name - The name of the texture
   * @param size - The size of the texture (width and height)
   * @param scene - The scene the texture belongs to
   */
  constructor(name: string, size: number, scene: any);
}

/**
 * Procedural texture that generates animated fire effects.
 * 
 * Creates realistic fire animations with customizable flame colors,
 * intensity, and movement patterns.
 */
export class FireProceduralTexture {
  /**
   * Creates a new fire procedural texture.
   * 
   * @param name - The name of the texture
   * @param size - The size of the texture (width and height)
   * @param scene - The scene the texture belongs to
   */
  constructor(name: string, size: number, scene: any);
}

/**
 * Procedural texture that generates grass patterns.
 * 
 * Creates grass-like textures with varying blade density and color variations
 * suitable for terrain rendering.
 */
export class GrassProceduralTexture {
  /**
   * Creates a new grass procedural texture.
   * 
   * @param name - The name of the texture
   * @param size - The size of the texture (width and height)
   * @param scene - The scene the texture belongs to
   */
  constructor(name: string, size: number, scene: any);
}

/**
 * Procedural texture that generates marble stone patterns.
 * 
 * Creates realistic marble textures with customizable vein patterns,
 * colors, and turbulence.
 */
export class MarbleProceduralTexture {
  /**
   * Creates a new marble procedural texture.
   * 
   * @param name - The name of the texture
   * @param size - The size of the texture (width and height)
   * @param scene - The scene the texture belongs to
   */
  constructor(name: string, size: number, scene: any);
}

/**
 * Procedural texture that generates normal maps from height data.
 * 
 * Converts height information into normal map data for realistic
 * surface detail and lighting effects.
 */
export class NormalMapProceduralTexture {
  /**
   * Creates a new normal map procedural texture.
   * 
   * @param name - The name of the texture
   * @param size - The size of the texture (width and height)
   * @param scene - The scene the texture belongs to
   */
  constructor(name: string, size: number, scene: any);
}

/**
 * Procedural texture that generates Perlin noise patterns.
 * 
 * Creates natural-looking random noise using the Perlin noise algorithm,
 * useful for terrain generation, cloud effects, and organic patterns.
 */
export class PerlinNoiseProceduralTexture {
  /**
   * Creates a new Perlin noise procedural texture.
   * 
   * @param name - The name of the texture
   * @param size - The size of the texture (width and height)
   * @param scene - The scene the texture belongs to
   */
  constructor(name: string, size: number, scene: any);
}

/**
 * Procedural texture that generates road surface patterns.
 * 
 * Creates asphalt-like textures with road markings, cracks,
 * and weathering effects.
 */
export class RoadProceduralTexture {
  /**
   * Creates a new road procedural texture.
   * 
   * @param name - The name of the texture
   * @param size - The size of the texture (width and height)
   * @param scene - The scene the texture belongs to
   */
  constructor(name: string, size: number, scene: any);
}

/**
 * Procedural texture that generates animated starfield patterns.
 * 
 * Creates space-like backgrounds with twinkling stars of varying
 * sizes and brightness levels.
 */
export class StarfieldProceduralTexture {
  /**
   * Creates a new starfield procedural texture.
   * 
   * @param name - The name of the texture
   * @param size - The size of the texture (width and height)
   * @param scene - The scene the texture belongs to
   */
  constructor(name: string, size: number, scene: any);
}

/**
 * Procedural texture that generates wood grain patterns.
 * 
 * Creates realistic wood textures with customizable grain direction,
 * ring patterns, and color variations.
 */
export class WoodProceduralTexture {
  /**
   * Creates a new wood procedural texture.
   * 
   * @param name - The name of the texture
   * @param size - The size of the texture (width and height)
   * @param scene - The scene the texture belongs to
   */
  constructor(name: string, size: number, scene: any);
}
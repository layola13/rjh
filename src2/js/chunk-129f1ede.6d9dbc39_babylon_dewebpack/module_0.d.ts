/**
 * Validates that a value is not empty or returns a validation error message.
 * 
 * @param value - The value to validate
 * @returns `true` if the value is truthy (valid), or the string "required" if validation fails
 * 
 * @example
 *
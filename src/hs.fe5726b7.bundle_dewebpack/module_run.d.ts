/**
 * Executes the onclick handler of an element if it exists.
 * 
 * @param element - The HTML element that may contain an onclick handler
 * @remarks
 * This function checks if the element has an onclick property defined,
 * and if so, invokes it. Commonly used in event delegation patterns.
 * 
 * @example
 *
/**
 * Asset module export for the top icon SVG file.
 * 
 * This module exports the public URL path to a static SVG asset that is
 * processed by the bundler. The asset is typically used for displaying
 * a "scroll to top" or similar UI element.
 * 
 * @module TopIconAsset
 */

/**
 * The public URL path to the top.svg icon asset.
 * 
 * This string contains the full path (including any CDN or public path prefix)
 * to the SVG file after bundling and asset optimization.
 * 
 * @example
 *
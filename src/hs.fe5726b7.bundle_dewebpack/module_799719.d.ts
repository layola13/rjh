/**
 * Asset module that exports the path to a UI XML configuration file.
 * This module provides a reference to a static UI asset used in the application.
 */

/**
 * The resolved path to the UI XML asset file.
 * This path is constructed by webpack during the build process and points to
 * the versioned/hashed asset file in the assets directory.
 * 
 * @example
 *
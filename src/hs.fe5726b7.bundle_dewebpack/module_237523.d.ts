/**
 * Grid asset module
 * Exports the path to a grid SVG asset file.
 * This module provides access to a static grid pattern image used in the application.
 */

/**
 * The public path to the grid SVG asset.
 * This path is resolved at build time and points to the grid pattern image
 * located in the assets directory.
 * 
 * @type {string}
 * @example
 *
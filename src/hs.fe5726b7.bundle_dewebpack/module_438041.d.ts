/**
 * Asset module for replace icon SVG file.
 * This module exports the public URL path to the replace icon asset.
 * Typically used in build systems to reference static assets with cache-busting hashes.
 */

/**
 * Public URL path to the replace icon SVG asset.
 * The path includes a content hash (6832f24f) for cache busting.
 * 
 * @example
 *
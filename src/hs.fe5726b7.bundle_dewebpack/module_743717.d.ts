/**
 * Front page v2 SVG asset module
 * 
 * This module exports the path to the front page version 2 SVG asset.
 * The asset is processed by the build system and includes a content hash
 * for cache busting.
 * 
 * @module FrontPageAsset
 */

/**
 * Path to the front page v2 SVG asset.
 * The path includes a content hash (232b1323) for cache invalidation.
 * 
 * @example
 *
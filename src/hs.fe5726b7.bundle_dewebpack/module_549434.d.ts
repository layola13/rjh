/**
 * Asset module that exports the path to the Icomoon font file.
 * This module provides the URL to the EOT (Embedded OpenType) font format,
 * primarily used for Internet Explorer compatibility.
 */

/**
 * The path to the Icomoon EOT font file.
 * Format: Embedded OpenType (.eot)
 * Hash: 0763e8d8 (content hash for cache busting)
 */
declare const icomoonFontPath: string;

export default icomoonFontPath;
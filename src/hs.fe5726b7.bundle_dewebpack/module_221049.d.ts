/**
 * Asset module for information icon SVG
 * 
 * This module exports the URL path to the information icon SVG asset.
 * The asset is processed and hashed by the build system for cache busting.
 * 
 * @module information-icon
 */

/**
 * The public URL path to the information.svg icon asset.
 * 
 * This path is resolved at build time and includes a content hash
 * (bf61d9a3) for efficient browser caching and cache invalidation.
 * 
 * @example
 *
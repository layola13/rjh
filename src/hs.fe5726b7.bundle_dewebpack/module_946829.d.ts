/**
 * Asset module that exports the path to the render failed SVG icon.
 * This is typically used to display an error state when rendering fails.
 * 
 * @module RenderFailedAsset
 */

/**
 * The public path to the render failed SVG asset.
 * This path is resolved at build time and points to the static asset location.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the editor SVG icon.
 * This module provides a reference to a static asset file used in the application's UI.
 * 
 * @module EditorAsset
 */

/**
 * The public path to the editor icon SVG file.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @example
 *
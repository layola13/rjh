/**
 * Asset module export for concrete hover pattern SVG
 * 
 * This module exports the URL path to a concrete hover pattern SVG asset.
 * The asset is typically used for UI hover effects or background patterns.
 * 
 * @module ConcreteHoverPattern
 */

/**
 * URL path to the concrete hover pattern SVG asset
 * 
 * This string represents the public URL where the SVG file can be accessed.
 * The path includes a content hash (f26b4347) for cache busting purposes.
 * 
 * @example
 *
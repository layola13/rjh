/**
 * Asset module that exports the path to a brick wall pattern SVG image.
 * This module provides the bundled URL for the brick_wall_pattern asset.
 * 
 * @module BrickWallPatternAsset
 */

/**
 * The public URL path to the brick wall pattern SVG asset.
 * This path is resolved at build time and includes the content hash for cache busting.
 * 
 * @example
 *
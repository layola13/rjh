/**
 * Sets whether the texture is a normal map.
 * 
 * Normal maps are used in 3D graphics to add surface detail without increasing polygon count.
 * They store directional information in RGB channels to simulate bumps and surface variations.
 * 
 * @param isNormalMap - True if this texture should be treated as a normal map, false otherwise
 */
function setIsNormalMap(isNormalMap: boolean): void;
/**
 * SVG asset module that exports the path to a "closed_normal" icon.
 * This asset is typically used in UI components to represent a closed or collapsed state.
 */

/**
 * The file path to the closed_normal SVG icon asset.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @example
 *
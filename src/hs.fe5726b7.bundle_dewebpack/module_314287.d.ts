/**
 * Asset module that exports the path to a gypsum wall pattern SVG file.
 * This module represents a static asset reference typically used in web applications
 * for displaying texture or background patterns.
 * 
 * @module GypsumWallPattern
 */

/**
 * The file path to the gypsum wall pattern SVG asset.
 * This path is resolved relative to the application's public path and includes
 * a content hash for cache-busting purposes.
 * 
 * @example
 *
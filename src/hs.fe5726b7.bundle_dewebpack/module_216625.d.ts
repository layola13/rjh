/**
 * Asset module that exports the path to the hatch background image.
 * This module provides access to a static image asset that has been processed
 * through the build pipeline with content hashing for cache busting.
 * 
 * @module HatchBackgroundAsset
 * @returns The public URL path to the hatch background image asset
 */

/**
 * The public URL path to the hatch background image.
 * The filename includes a content hash (26b1e4cb) for cache invalidation.
 * 
 * @constant
 * @type {string}
 * @example
 *
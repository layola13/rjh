/**
 * Asset module that exports the path to the Japanese (ja_JP) localization resource file.
 * This module provides a reference to a JSON resource containing Japanese translations or locale-specific data.
 * 
 * @module LocaleResourceJapanese
 */

/**
 * The public path to the Japanese locale resource JSON file.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @type {string}
 * @example "assets/resource_ja_JP.f597b113.json"
 */
declare const localeResourcePath: string;

export default localeResourcePath;
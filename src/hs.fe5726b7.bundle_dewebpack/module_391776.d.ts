/**
 * Asset module that exports the path to the arrow_for_camera_clip SVG file.
 * This module provides the public URL path to a static SVG asset used for camera clip UI elements.
 * 
 * @module CameraClipArrowAsset
 */

/**
 * The public URL path to the arrow_for_camera_clip.svg asset.
 * This path is resolved at build time and points to the optimized asset in the distribution folder.
 * 
 * @example
 *
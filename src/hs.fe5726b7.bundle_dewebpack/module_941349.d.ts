/**
 * Webpack asset module that exports the public URL path for the thumbup_selected SVG icon.
 * This module resolves to the bundled asset path at runtime.
 * 
 * @module assets/thumbup_selected
 */

/**
 * The public URL path to the thumbup_selected.svg asset.
 * This path is resolved by the bundler and points to the optimized asset location.
 * 
 * File: thumbup_selected.fa88243a.svg (with content hash for cache busting)
 */
declare const assetPath: string;

export default assetPath;
/**
 * SVG asset module for endpoint icon
 * 
 * This module exports the URL path to a static SVG asset that represents
 * an endpoint icon used throughout the application.
 * 
 * @module EndpointAsset
 */

/**
 * The URL path to the endpoint SVG icon asset.
 * 
 * This path is resolved at build time and points to the optimized
 * SVG file in the assets directory. The hash in the filename (f350277d)
 * ensures cache busting when the asset is updated.
 * 
 * @example
 *
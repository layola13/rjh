/**
 * Asset module that exports the path to the offset cursor SVG icon.
 * This is typically used for custom cursor implementations in the UI.
 * 
 * @module AssetExports
 */

/**
 * The public path to the offset cursor SVG asset.
 * This path is resolved at build time and points to the static asset location.
 * 
 * @example
 *
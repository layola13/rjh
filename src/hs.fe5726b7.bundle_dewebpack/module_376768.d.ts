/**
 * SVG asset module export
 * Exports the path to the completed icon SVG asset
 */

/**
 * The file path to the completed.svg asset
 * @remarks
 * This path is resolved at build time and points to the hashed SVG file
 * in the assets directory
 */
declare const completedIconPath: string;

export default completedIconPath;
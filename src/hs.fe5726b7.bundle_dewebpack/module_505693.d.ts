/**
 * Asset module that exports the path to the select icon SVG file.
 * This is typically used for UI components that need to reference the select dropdown icon.
 * 
 * @module SelectIconAsset
 */

/**
 * The public path to the select icon SVG asset.
 * This path is resolved at build time and points to the compiled asset location.
 * 
 * @example
 *
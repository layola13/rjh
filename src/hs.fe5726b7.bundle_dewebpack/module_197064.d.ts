/**
 * CSS module definition for large view panel component
 * Defines styles for a fixed-position container with high z-index
 */

/**
 * CSS class names exported by this module
 */
export interface LargeViewPanelStyles {
  /**
   * Container for large view panel
   * - Uses fixed positioning
   * - Z-index: 2400 (renders above most UI elements)
   */
  'largeview-panel-container': string;
}

/**
 * CSS module exports
 * Import this in components to access type-safe CSS class names
 * 
 * @example
 *
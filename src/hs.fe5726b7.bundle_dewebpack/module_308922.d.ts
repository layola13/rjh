/**
 * SVG asset module that exports the path to a hover icon.
 * This module provides access to a static SVG asset used for hover states in the UI.
 * 
 * @module HoverAssetModule
 */

/**
 * The public path to the hover SVG asset.
 * This path is resolved at build time and points to the compiled asset location.
 * 
 * @example
 *
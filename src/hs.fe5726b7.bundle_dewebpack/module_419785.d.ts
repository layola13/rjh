/**
 * Asset module that exports the path to the contact us SVG file.
 * This is typically used for importing static assets in a web application.
 * 
 * @module ContactUsAsset
 * @example
 * import contactUsIcon from './contactus-asset';
 * // contactUsIcon will be "assets/contactus.dae2461f.svg"
 */

/**
 * The public path to the contact us SVG asset.
 * The hash in the filename (dae2461f) is typically generated during the build process
 * for cache-busting purposes.
 * 
 * @constant {string} contactUsAssetPath - Relative path to the SVG asset
 */
declare const contactUsAssetPath: string;

export default contactUsAssetPath;
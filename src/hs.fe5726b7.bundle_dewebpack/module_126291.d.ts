/**
 * 默认资源路径模块
 * 
 * 该模块导出默认的 3D 资源文件路径。
 * 原始模块通过 Webpack 的 public path 拼接静态资源路径。
 */

/**
 * 默认 T3D 资源文件的路径
 * 
 * @remarks
 * 此路径指向预构建的默认 3D 资源文件，通常用于：
 * - 作为场景的默认加载资源
 * - 资源加载失败时的降级方案
 * - 初始化时的占位资源
 * 
 * @example
 *
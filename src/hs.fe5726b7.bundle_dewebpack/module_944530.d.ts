/**
 * Asset module that exports the path to the bottom arrow SVG icon.
 * This module provides a reference to a static SVG asset used in the application UI.
 * 
 * @module BottomIconAsset
 * @since 1.0.0
 */

/**
 * The relative path to the bottom arrow SVG icon asset.
 * The path is resolved relative to the application's public asset directory.
 * 
 * @constant
 * @type {string}
 * @example
 *
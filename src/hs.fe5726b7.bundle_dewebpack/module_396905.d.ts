/**
 * Asset module that exports the path to an SVG arrow icon.
 * This module represents a static asset reference typically handled by a bundler.
 * 
 * @module ArrowIcon
 */

/**
 * The public path to the arrow315 SVG asset.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @example
 *
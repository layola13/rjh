/**
 * Asset reference module for Italian (Italy) localization resources.
 * 
 * This module provides the runtime path to the Italian language resource file,
 * which contains translations and locale-specific content for the it_IT locale.
 * 
 * @module LocaleResourceItIT
 */

/**
 * The public path to the Italian (Italy) locale resource JSON file.
 * This path is resolved at build time and points to the compiled asset.
 * 
 * @example
 *
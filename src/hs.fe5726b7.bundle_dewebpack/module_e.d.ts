/**
 * Error handler utility that re-throws the provided error.
 * 
 * This function can be used as a simple error propagation mechanism,
 * typically in error handling pipelines or as a callback for error events.
 * 
 * @param error - The error to be re-thrown
 * @throws Always throws the provided error
 * 
 * @example
 *
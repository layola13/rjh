/**
 * Webpack asset module export
 * Exports the public URL path for an SVG asset (editable icon)
 */

/**
 * The public URL path to the editable.svg icon asset.
 * This path is resolved at build time by the bundler and points to the
 * hashed SVG file in the assets directory.
 * 
 * @example
 *
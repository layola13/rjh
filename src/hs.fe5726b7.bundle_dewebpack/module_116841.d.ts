/**
 * Logo asset path module
 * Exports the public path to the application's logo image asset
 */

/**
 * The relative path to the logo image asset
 * @remarks This path is typically resolved by the bundler's public path configuration
 */
declare const logoAssetPath: string;

export default logoAssetPath;
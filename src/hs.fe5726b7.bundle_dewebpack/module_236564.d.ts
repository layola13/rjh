/**
 * Concrete selected pattern asset module
 * Exports the public path to the concrete_selected_pattern SVG asset
 */

/**
 * The public URL path to the concrete selected pattern SVG asset.
 * This path is resolved at build time and points to the optimized asset location.
 * 
 * @example
 *
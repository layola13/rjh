/**
 * Event handler that prevents default action on clicks within disabled UI elements.
 * 
 * This handler is typically attached to anchor tags (<a>) that are children of
 * elements with the .ui-state-disabled class, preventing navigation or other
 * default behaviors when the parent element is in a disabled state.
 * 
 * @param event - The mouse event triggered by clicking on a link inside a disabled UI element
 * @returns void
 * 
 * @example
 *
/**
 * Structure transaction type definitions for HSW (Homesite Web) system.
 * Defines all available transaction types for structure and beam operations.
 * @module StructureTransactionTypes
 */

/**
 * Enumeration of all structure and beam transaction types used in the HSW transaction system.
 * Each property maps to a unique transaction identifier string.
 * 
 * @remarks
 * This object is frozen and immutable at runtime to prevent accidental modifications.
 * 
 * @example
 *
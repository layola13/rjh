/**
 * Asset module export for concrete pattern SVG
 * 
 * This module provides the public path to a static SVG asset.
 * The asset is processed by the build system and includes a content hash
 * for cache busting purposes.
 * 
 * @module ConcretePatternAsset
 */

/**
 * The public URL path to the concrete pattern SVG asset.
 * 
 * This path is relative to the application's public directory and includes
 * a content hash (60de4d1a) for cache invalidation when the file changes.
 * 
 * @example
 *
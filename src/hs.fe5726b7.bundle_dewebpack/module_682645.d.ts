/**
 * Asset module exporting the path to the default switch symbol SVG file.
 * This module provides the bundled asset path for a switch UI component icon.
 * 
 * @module DefaultSwitchSymbol
 */

/**
 * The public URL path to the default switch symbol SVG asset.
 * This path is resolved at build time and points to the static asset location.
 * 
 * @example
 *
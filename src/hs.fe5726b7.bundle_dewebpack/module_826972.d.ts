/**
 * Asset module export for pin icon SVG file
 * 
 * This module provides the public URL path to the pin.svg asset.
 * The hash (429f5136) ensures cache busting when the asset changes.
 * 
 * @module assets/pin
 */

/**
 * Public URL path to the pin SVG icon asset
 * 
 * This path is resolved at build time and points to the optimized
 * SVG file in the assets directory with a content hash for versioning.
 * 
 * @example
 *
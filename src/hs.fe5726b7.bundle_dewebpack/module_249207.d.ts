/**
 * Asset module that exports the path to the close icon SVG file.
 * This module provides access to a static asset (close button icon) that is
 * processed by the build system and available at runtime with a content hash
 * for cache busting.
 */

/**
 * The public URL path to the close icon SVG asset.
 * The path includes a content hash (ca56a7a2) for cache invalidation.
 * 
 * @example
 *
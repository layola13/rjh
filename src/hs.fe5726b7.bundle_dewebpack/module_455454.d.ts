/**
 * Asset module that exports the path to the fold icon SVG file.
 * This module provides access to a static asset (fold.svg) that has been
 * processed by the build system and given a content hash for cache busting.
 * 
 * @module FoldIconAsset
 */

/**
 * The public URL path to the fold icon SVG asset.
 * The hash in the filename (5d70bb18) ensures proper cache invalidation
 * when the file content changes.
 * 
 * @example
 *
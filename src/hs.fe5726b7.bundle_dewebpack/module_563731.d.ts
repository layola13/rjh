/**
 * Gypsum wall selected pattern SVG asset
 * 
 * This module exports the URL path to the gypsum wall selected pattern SVG image.
 * The asset is processed by webpack and the final URL includes a content hash for cache busting.
 * 
 * @module GypsumWallSelectedPattern
 * @example
 *
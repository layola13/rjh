/**
 * Webpack静态资源导出模块
 * 
 * 此模块导出一个SVG图标文件的公共URL路径。
 * 在构建时，Webpack会将资源文件复制到输出目录，并通过publicPath生成完整的访问路径。
 */

/**
 * SVG上传图标的公共访问路径
 * 
 * @remarks
 * - 资源文件名: upload.7acba042.svg
 * - 文件哈希: 7acba042 (用于缓存破坏)
 * - 该路径会根据Webpack配置的publicPath动态生成
 * 
 * @example
 *
/**
 * Webpack asset module export
 * Exports the public URL path for the global logo image asset
 */

/**
 * The public URL path to the global logo PNG image.
 * This path is resolved at build time by the bundler and points to the hashed asset file.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the delete tiles SVG icon.
 * This module provides a typed reference to a static asset file used in the application.
 * 
 * @module assets/tiles_delete
 */

/**
 * The public path to the delete tiles SVG icon asset.
 * This path is resolved at build time and points to the optimized asset location.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the timeControl SVG icon.
 * This module provides access to a static SVG asset used for time control UI elements.
 * 
 * @module TimeControlAsset
 */

/**
 * The public path to the timeControl SVG asset.
 * This path is resolved at build time and points to the bundled SVG file.
 * 
 * @example
 *
/**
 * Checks if the current JavaScript environment supports native ES6 class inheritance
 * using Reflect.construct. This is used to determine if classes can be properly extended
 * from built-in types like Boolean, Array, etc.
 * 
 * @returns {boolean} True if the environment supports native class inheritance with Reflect.construct
 * 
 * @example
 *
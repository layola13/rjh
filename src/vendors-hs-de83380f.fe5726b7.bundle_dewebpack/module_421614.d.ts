/**
 * Extracts the first element of an array if the input is an array.
 * This is typically used as part of array destructuring helper utilities.
 * 
 * @template T - The type of array elements
 * @param value - The value to check and potentially extract from
 * @returns The original array if input is an array, undefined otherwise
 * 
 * @example
 *
/**
 * Object assignment utility that merges properties from source objects into a target object.
 * Polyfill for Object.assign that ensures compatibility across environments.
 * 
 * @param target - The target object to merge properties into
 * @param sources - One or more source objects whose properties will be copied
 * @returns The target object with merged properties
 * 
 * @example
 *
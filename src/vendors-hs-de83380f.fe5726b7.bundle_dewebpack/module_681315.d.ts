/**
 * Traverses the prototype chain to find the nearest ancestor object that has the specified property.
 * 
 * @param obj - The object to start searching from
 * @param propertyName - The property name to search for
 * @returns The first object in the prototype chain that owns the property, or null if not found
 * 
 * @example
 *
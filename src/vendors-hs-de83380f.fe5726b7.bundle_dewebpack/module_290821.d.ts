/**
 * Re-export module from internal dependency
 * This module acts as a barrel export, forwarding the default export from another module.
 */

/**
 * The default export type from the re-exported module.
 * Replace `unknown` with the actual type if the source module type is known.
 */
declare const _default: unknown;

export default _default;
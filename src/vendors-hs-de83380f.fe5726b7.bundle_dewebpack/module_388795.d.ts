/**
 * Gets the prototype of an object.
 * Uses Object.getPrototypeOf if available, otherwise falls back to __proto__ access.
 * 
 * @param obj - The object whose prototype is to be retrieved
 * @returns The prototype of the given object
 * 
 * @example
 *
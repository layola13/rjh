/**
 * Creates an iterator that traverses object keys in reverse insertion order.
 * 
 * @template T - The type of the object being iterated
 * @param obj - The object whose keys will be iterated
 * @returns An iterator function that yields keys in reverse order
 * 
 * @example
 *
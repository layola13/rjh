/**
 * Type guard to check if a value is an array and return it unchanged.
 * This function serves as an identity function for arrays, returning the array
 * if the input is indeed an array, otherwise returning undefined.
 * 
 * @template T - The type of elements in the array
 * @param value - The value to check
 * @returns The original array if the value is an array, undefined otherwise
 * 
 * @example
 *
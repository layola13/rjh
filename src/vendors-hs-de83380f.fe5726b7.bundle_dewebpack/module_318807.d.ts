/**
 * Converts a value to a primitive property key (string or symbol).
 * 
 * @param value - The value to convert to a property key
 * @returns A string or symbol that can be used as an object property key
 * 
 * @example
 *
/**
 * Converts array-like or iterable objects to arrays.
 * Handles strings, Maps, Sets, Arguments objects, and typed arrays.
 * 
 * @param value - The array-like or iterable object to convert
 * @param length - Optional length to limit the resulting array
 * @returns An array representation of the input, or undefined if conversion is not possible
 * 
 * @example
 *
/**
 * Converts a value to a property key (string or symbol).
 * 
 * This function implements the ECMAScript ToPrimitive abstract operation
 * for converting values to property keys suitable for object property access.
 * 
 * @param value - The value to convert to a property key
 * @returns A string or symbol that can be used as an object property key
 * 
 * @example
 *
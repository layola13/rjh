/**
 * Module: module_tqIv
 * 
 * This module re-exports the contents from another module (original webpack module ID: 978734).
 * Without access to the actual module implementation, the exact type definition cannot be determined.
 * 
 * @module module_tqIv
 */

/**
 * The exported content from the referenced module.
 * Replace `unknown` with the actual type once the source module is identified.
 */
declare const moduleContent: unknown;

export = moduleContent;

// Alternative if it's an ES module with named/default exports:
// export * from './actual-module-path';
// export { default } from './actual-module-path';
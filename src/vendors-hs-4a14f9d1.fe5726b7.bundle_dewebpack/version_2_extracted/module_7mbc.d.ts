/**
 * Assertion utility function that throws an error if a condition is falsy.
 * Commonly used for runtime validation and invariant checking.
 * 
 * @param condition - The condition to assert. If falsy, an error is thrown.
 * @param message - Optional error message. Defaults to "Assert Failed" if not provided.
 * @throws {Error} When the condition is falsy.
 * 
 * @example
 *
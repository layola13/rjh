/**
 * Assertion utility that throws an error if the condition is false.
 * 
 * @param condition - The condition to assert. If falsy, an error will be thrown.
 * @param message - Optional custom error message. Defaults to "Assert Failed" if not provided.
 * @throws {Error} Throws an Error when the condition is falsy.
 * 
 * @example
 *
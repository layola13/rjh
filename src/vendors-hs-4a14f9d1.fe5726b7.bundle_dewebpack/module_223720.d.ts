/**
 * HSMeta module type definitions
 * 
 * This module exports the HSMeta type/class from module 484838.
 * HSMeta is assigned to the global namespace under `e.g.HSMeta`.
 * 
 * @module HSMeta
 */

/**
 * HSMeta interface or class
 * 
 * Note: Without access to module 484838's implementation,
 * this is a placeholder type definition. Please replace with
 * actual type information based on the source module.
 */
export interface HSMeta {
  // Add actual properties and methods here based on module 484838
  [key: string]: unknown;
}

/**
 * Default export of the HSMeta module
 */
declare const HSMeta: HSMeta;

export default HSMeta;

/**
 * Global augmentation for the HSMeta type
 */
declare global {
  namespace HSMeta {
    // Add namespace members if applicable
  }
}
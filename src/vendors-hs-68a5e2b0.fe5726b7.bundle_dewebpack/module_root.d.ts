/**
 * Retrieves the first element node from the document's child nodes.
 * 
 * @remarks
 * This method iterates through all child nodes of the DOM document and returns
 * the first node that is of type ELEMENT_NODE, wrapped in an Ia instance.
 * 
 * @returns The first element child wrapped in an Ia instance, or undefined if no element nodes exist
 * 
 * @example
 *
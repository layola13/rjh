/**
 * Parses a version string array into numeric components.
 * Converts string representations of version numbers into integers.
 * 
 * @param versionParts - Array containing version string components (e.g., ['1', '2', '3'])
 * @returns Tuple of three integers representing [major, minor, patch] version numbers
 * 
 * @example
 *
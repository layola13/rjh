/**
 * Click event handler that prevents event propagation and executes custom logic.
 * 
 * @remarks
 * This handler stops the event from bubbling up the DOM tree and delegates
 * to a custom handler function M for additional processing.
 * 
 * @param event - The mouse event triggered by the click interaction
 * 
 * @example
 *
/**
 * Error handling module
 * Handles application-level error events
 * @module onError
 */

/**
 * Registers or triggers an error event handler
 * @param eventType - The type of event to handle (typically "error")
 */
declare function onError(): void;

export default onError;
/**
 * 判断两个数组是否完全相等（浅比较）
 * 
 * 通过逐个比较数组元素来判断两个数组是否相等。
 * 使用严格相等（===）进行元素比较。
 * 
 * @param first - 第一个待比较的数组
 * @param second - 第二个待比较的数组
 * @returns 如果数组长度相同且所有元素严格相等则返回 true，否则返回 false
 * 
 * @remarks
 * - 如果两个参数引用同一个数组对象，直接返回 true
 * - 如果任一参数为 null 或 undefined，返回 false
 * - 只进行浅层比较，不递归比较嵌套对象或数组
 * 
 * @example
 *
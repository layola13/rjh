/**
 * 延迟状态更新的自定义 Hook
 * 
 * 提供一个带有延迟功能的状态管理 Hook，可以在指定延迟后更新状态。
 * 支持取消待处理的更新，并在组件卸载时自动清理定时器。
 * 
 * @param delay - 延迟时间（毫秒），默认为 10ms
 * @returns 包含三个元素的元组：
 *   - [0] 当前状态值
 *   - [1] 延迟更新函数
 *   - [2] 取消待处理更新的函数
 * 
 * @example
 *
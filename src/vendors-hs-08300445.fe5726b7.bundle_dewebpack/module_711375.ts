/**
 * Re-exports the default export from module 251574
 * 
 * This module serves as a barrel export, forwarding the default export
 * from the underlying module (originally module ID: 251574).
 * 
 * @module module_711375
 * @remarks
 * The original code was a Webpack module wrapper that:
 * - Imported helpers from module 283673 (likely tslib or similar)
 * - Re-exported the default export from module 251574
 * 
 * Without access to the source module, the exact type is unknown.
 * Replace `unknown` with the actual type when available.
 */

/**
 * Default export re-exported from the underlying module
 * 
 * @remarks
 * To provide accurate types, inspect module 251574's actual exports.
 * This could be a class, function, object, or any other value.
 */
declare const _default: unknown;

export default _default;

/**
 * Alternative: If you know the actual type, replace unknown with it.
 * Examples:
 * 
 * // If it's a React component:
 * // declare const _default: React.ComponentType<YourPropsInterface>;
 * 
 * // If it's a class:
 * // declare const _default: typeof YourClass;
 * 
 * // If it's a function:
 * // declare const _default: (param: ParamType) => ReturnType;
 * 
 * // If it's a configuration object:
 * // interface Config { ... }
 * // declare const _default: Config;
 */
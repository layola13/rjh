/**
 * React context for module state management.
 * Provides a shared context object for component tree communication.
 */

import type { Context } from 'react';

/**
 * Default context instance created with an empty object as initial value.
 * This context can be used to share state across React component hierarchies
 * without prop drilling.
 * 
 * @example
 *
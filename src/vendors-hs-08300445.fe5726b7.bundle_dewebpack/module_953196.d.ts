/**
 * Checks if the code is running in a browser environment.
 * 
 * This utility function detects whether the current execution context
 * has access to browser-specific APIs (window, document, and DOM manipulation).
 * 
 * @returns {boolean} `true` if running in a browser environment with DOM support, `false` otherwise
 * 
 * @example
 *
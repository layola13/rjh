/**
 * Checks if the code is running in a browser environment.
 * 
 * This function determines whether the current execution context is a browser
 * by checking for the existence of the `window` object and its `document` property.
 * 
 * @returns {boolean} `true` if running in a browser environment with DOM support, `false` otherwise
 * 
 * @example
 *
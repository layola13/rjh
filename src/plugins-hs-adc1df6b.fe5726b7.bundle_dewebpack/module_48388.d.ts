/**
 * SVG asset module - Read-only icon
 * 
 * This module exports the URL path to a read-only icon SVG asset.
 * The asset is typically used to indicate read-only state in UI components.
 * 
 * @module ReadOnlyIcon
 */

/**
 * The public URL path to the read-only icon SVG asset.
 * 
 * The path is resolved at build time and includes content hash for cache busting.
 * 
 * @example
 *
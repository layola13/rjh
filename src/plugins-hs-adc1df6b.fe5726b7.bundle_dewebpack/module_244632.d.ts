/**
 * CSS Module Type Definition
 * 
 * This module represents a dynamically imported CSS module with style injection capabilities.
 * It handles style tag transformation, DOM insertion, and provides typed access to CSS class names.
 */

/**
 * CSS Module Locals
 * 
 * Represents the mapping of CSS class names to their hashed/scoped equivalents.
 * Each key is the original class name, and the value is the transformed class name.
 * 
 * @example
 *
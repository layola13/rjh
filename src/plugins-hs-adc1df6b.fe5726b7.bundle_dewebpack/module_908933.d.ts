/**
 * Asset module export for master_basic image
 * 
 * This module provides the URL path to the master_basic.png asset.
 * The asset is processed and versioned by the build system with a content hash.
 * 
 * @module assets/master_basic
 */

/**
 * URL path to the master_basic image asset.
 * The filename includes a content hash (3d287fe0) for cache busting.
 * 
 * @constant
 * @type {string}
 * @example
 *
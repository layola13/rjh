/**
 * CSS Module Type Definition
 * 
 * This module represents a CSS module with locally scoped class names.
 * It re-exports all named exports from the base CSS module and provides
 * a default export containing the generated class name mappings.
 */

/**
 * CSS Module class name mapping
 * 
 * Contains locally scoped CSS class names as key-value pairs.
 * Keys represent the original class names in the CSS file,
 * values are the generated unique class names.
 * 
 * @example
 *
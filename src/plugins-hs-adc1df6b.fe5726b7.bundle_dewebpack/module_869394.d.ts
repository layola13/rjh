/**
 * Asset module that exports the path to the overdue background image.
 * This module is typically used in build systems to reference static assets
 * with cache-busting hashes in their filenames.
 * 
 * @module OverdueBackgroundAsset
 */

/**
 * The relative or absolute URL path to the overdue background image asset.
 * The filename includes a content hash (b2f44ed2) for cache busting purposes.
 * 
 * @example
 *
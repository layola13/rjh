/**
 * Window pocket SVG asset module
 * 
 * This module provides the public URL path to the window pocket SVG icon.
 * The asset is processed by the build system and the exported string
 * contains the final public path with content hash for cache busting.
 * 
 * @module windowpocket-asset
 * @example
 *
/**
 * Asset module that exports the path to the private icon SVG file.
 * This module is typically used to import static assets in a webpack-bundled application.
 * 
 * @module PrivateIconAsset
 */

/**
 * The public path to the private icon SVG asset.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @example
 *
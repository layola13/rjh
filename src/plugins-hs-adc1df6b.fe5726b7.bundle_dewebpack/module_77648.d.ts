/**
 * Asset module for add_my_group SVG icon
 * Exports the public URL path to the SVG asset
 */

/**
 * Public URL path to the add_my_group.svg icon asset
 * 
 * This path is resolved at build time and points to the static asset
 * in the assets directory. The hash (71a32bca) ensures cache busting
 * when the file content changes.
 * 
 * @example
 *
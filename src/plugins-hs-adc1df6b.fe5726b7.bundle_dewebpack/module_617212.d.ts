/**
 * Asset module for the complete hover state SVG icon.
 * This module exports the path to a static SVG asset used for displaying
 * the hover state of a completion indicator in the UI.
 * 
 * @module CompleteHoverAsset
 */

/**
 * The file path to the complete hover SVG asset.
 * Points to a static SVG resource located in the assets directory.
 * 
 * @remarks
 * This path is resolved at build time and includes a content hash (10a4e838)
 * for cache busting purposes.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the pro_plus PNG image.
 * This file represents a static asset reference in the build system.
 */

/**
 * The public path URL to the pro_plus image asset.
 * This path is resolved at build time and points to the optimized image file.
 * 
 * @example
 * import proPlus from './pro_plus';
 * <img src={proPlus} alt="Pro Plus" />
 */
declare const assetPath: string;

export default assetPath;
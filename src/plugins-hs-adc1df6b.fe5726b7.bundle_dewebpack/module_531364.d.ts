/**
 * Webpack asset reference module
 * Exports the public path to a static image asset (floorplan icon)
 */

/**
 * The public URL path to the floorplan icon image asset.
 * This path is resolved by the bundler and points to the hashed asset file.
 * 
 * @example
 *
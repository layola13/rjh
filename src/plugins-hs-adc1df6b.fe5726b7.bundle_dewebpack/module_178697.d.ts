/**
 * Asset module that exports the path to the mark-disable icon image.
 * This module provides the URL/path to a disabled mark icon asset.
 * 
 * @module MarkDisableAsset
 */

/**
 * The file path or URL to the mark-disable.png asset.
 * This is typically used for displaying a disabled state mark icon in the UI.
 * 
 * The actual path is resolved by the build system and includes a content hash
 * for cache busting (e.g., "assets/mark-disable.efa8a0bf.png").
 */
declare const markDisableAssetPath: string;

export default markDisableAssetPath;
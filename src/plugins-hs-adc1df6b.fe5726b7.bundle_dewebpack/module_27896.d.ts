/**
 * Asset module for holepocket SVG icon
 * 
 * This module exports the path to a static SVG asset that is processed
 * during the build step. The asset is typically used for rendering
 * hole pocket icons in the UI.
 * 
 * @module assets/holepocket
 */

/**
 * Path to the holepocket SVG asset
 * 
 * The path is resolved relative to the public assets directory
 * and includes a content hash for cache busting.
 * 
 * @example
 *
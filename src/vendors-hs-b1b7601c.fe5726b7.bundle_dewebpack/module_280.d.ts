/**
 * 获取对象的可枚举属性键名数组（排除constructor）
 * 
 * @remarks
 * 此函数用于提取对象自身的可枚举属性名称。
 * - 如果对象不是类数组对象，则使用备用方法获取所有键
 * - 否则遍历对象自身属性，排除 'constructor' 属性
 * 
 * @param value - 要提取属性键的目标对象
 * @returns 包含对象自身可枚举属性名的字符串数组
 * 
 * @example
 *
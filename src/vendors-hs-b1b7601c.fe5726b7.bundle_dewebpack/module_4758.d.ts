/**
 * Checks if a key exists in the internal data structure.
 * 
 * This is typically used in collection-like classes (Map, Set, Cache, etc.)
 * to determine whether a specific key is present in the stored data.
 * 
 * @template K - The type of the key
 * @param key - The key to check for existence
 * @returns True if the key exists in the data structure, false otherwise
 * 
 * @example
 *
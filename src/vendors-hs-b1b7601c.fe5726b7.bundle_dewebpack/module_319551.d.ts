/**
 * Creates an object by mapping arrays of keys and values together.
 * 
 * @template K - The type of the keys (must be valid object key types)
 * @template V - The type of the values
 * 
 * @param keys - Array of keys for the resulting object
 * @param values - Array of values corresponding to each key
 * @returns An object where each key from the keys array is mapped to the corresponding value from the values array
 * 
 * @example
 *
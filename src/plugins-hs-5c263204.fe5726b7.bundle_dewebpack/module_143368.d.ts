/**
 * CSS Module type definitions
 * 
 * This module represents a CSS-in-JS module that exports:
 * - A default export containing CSS class name mappings (CSS Modules locals)
 * - Named exports for each individual class name from the CSS file
 * 
 * @module CSSModule
 */

/**
 * CSS Modules class name mapping object.
 * Maps original class names to their scoped/hashed equivalents.
 * 
 * @example
 *
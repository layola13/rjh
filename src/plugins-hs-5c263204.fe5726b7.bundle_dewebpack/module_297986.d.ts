/**
 * Webpack asset module that exports the URL path to a dropup SVG icon.
 * This module provides access to a static SVG asset that is typically used
 * for UI components indicating upward-oriented dropdown menus or expandable sections.
 * 
 * @module DropupAssetModule
 */

/**
 * The public URL path to the dropup SVG icon asset.
 * This path is resolved at build time and points to the hashed asset file.
 * 
 * @constant
 * @type {string}
 * @example
 *
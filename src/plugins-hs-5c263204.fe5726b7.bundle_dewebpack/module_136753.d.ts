/**
 * Asset module export for rotate.svg
 * 
 * This module exports the URL path to a static SVG asset (rotate icon).
 * The asset is typically processed by a bundler and placed in the assets directory
 * with a content hash for cache busting.
 * 
 * @module RotateAsset
 */

/**
 * The URL path to the rotate SVG icon asset.
 * 
 * This path is generated at build time and includes a content hash (4b3b1bab)
 * for optimal browser caching. The actual file will be served from the
 * configured public path with the pattern: assets/rotate.[hash].svg
 * 
 * @example
 *
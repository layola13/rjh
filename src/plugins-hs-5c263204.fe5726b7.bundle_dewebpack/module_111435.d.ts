/**
 * Asset module declaration for group SVG icon
 * 
 * This module exports the public URL path to a static SVG asset.
 * The asset is processed by the bundler and the path is resolved at build time.
 * 
 * @module GroupIconAsset
 */

/**
 * Public URL path to the group icon SVG asset.
 * 
 * The actual file is located at `assets/group.2732f41c.svg` where the hash
 * `2732f41c` is typically generated for cache-busting purposes.
 * 
 * @example
 *
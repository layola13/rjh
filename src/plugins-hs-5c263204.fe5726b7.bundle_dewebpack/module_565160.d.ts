/**
 * Array of submit data handler functions.
 * Used to store and manage callbacks that process form submission data.
 */
export declare const submitDataHandleFunctionArr: Array<(...args: any[]) => any>;
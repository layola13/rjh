/**
 * Asset module that exports the path to the delete icon SVG file.
 * This module provides a reference to a static asset (delete icon) used in the application.
 * 
 * @module DeleteIconAsset
 */

/**
 * The public path to the delete icon SVG asset.
 * This path is resolved at build time and points to the compiled asset location.
 * 
 * @example
 *
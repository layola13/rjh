/**
 * Asset module that exports the path to a background image.
 * This module provides the resolved URL path to the bg.png asset after bundling.
 */

/**
 * The resolved path to the background image asset.
 * Points to the bundled image file with content hash in the filename.
 * 
 * @example
 *
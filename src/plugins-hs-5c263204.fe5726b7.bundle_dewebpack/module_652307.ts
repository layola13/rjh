const feedbackBlockStyles = `
.feedback-block-wrapper {
    display: block;
    width: 100%;
    background: #FFFFFF;
    box-shadow: 0 2px 50px 0 rgba(0, 0, 0, 0.03);
    border-radius: 8px;
    padding: 20px 18px;
}

.feedback-block-wrapper.feedback-black {
    background: rgba(255, 255, 255, 0.1);
}
`;

export default feedbackBlockStyles;
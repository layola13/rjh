/**
 * Asset path declaration for recommendation arrow hover SVG
 * 
 * This module exports the public path to the recommendation arrow hover icon asset.
 * The asset is processed and versioned by the build system with a content hash.
 * 
 * @module AssetPaths
 */

/**
 * Public URL path to the recommendation arrow hover SVG asset.
 * 
 * This path is resolved by the build system and includes:
 * - Base public path from webpack configuration
 * - Asset directory structure (assets/)
 * - Filename with content hash for cache busting (5da38395)
 * 
 * @example
 *
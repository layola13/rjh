/**
 * Webpack asset module that exports the URL path to the home image asset.
 * This module represents a static image import that has been processed by Webpack's asset loader.
 * The hash in the filename (339093b6) is used for cache busting.
 */

/**
 * The resolved URL path to the home.png image asset.
 * This path is relative to the application's public path and includes a content hash
 * for optimal browser caching.
 * 
 * @example
 *
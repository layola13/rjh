/**
 * Asset module that exports the path to the recommendation helper SVG icon.
 * This module is typically used to reference static assets in a type-safe manner.
 * 
 * @module RecommendationHelperAsset
 */

/**
 * The public path to the recommendation helper SVG asset.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @example
 *
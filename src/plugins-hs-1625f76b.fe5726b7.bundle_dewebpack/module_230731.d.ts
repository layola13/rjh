/**
 * Asset module that exports the path to the replacedoors SVG file.
 * This module provides a static reference to the SVG asset used in the application.
 * 
 * @module AssetModule
 */

/**
 * The public path to the replacedoors SVG asset.
 * This path is typically resolved by the build system and points to the
 * final location of the asset in the production build.
 * 
 * @constant
 */
export const REPLACE_DOORS_SVG_PATH: string = "assets/replacedoors.66feeecf.svg";

/**
 * Default export of the asset path for compatibility with CommonJS-style imports.
 */
export default REPLACE_DOORS_SVG_PATH;
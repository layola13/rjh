/**
 * Loading icon component props interface
 */
export interface LoadingIconProps {
  /**
   * Controls the visibility of the loading icon
   * @default true
   */
  show?: boolean;

  /**
   * Additional CSS class names to apply to the component
   */
  className?: string;
}

/**
 * Loading icon component that displays a loading spinner/animation
 * 
 * @example
 *
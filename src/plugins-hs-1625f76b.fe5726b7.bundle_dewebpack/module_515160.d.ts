/**
 * Asset module that exports the path to the merchant model SVG image.
 * This module provides the public URL path for the merchant_model.66582811.svg asset.
 * 
 * @module MerchantModelAsset
 */

/**
 * The public URL path to the merchant model SVG asset.
 * Used for displaying merchant-related visual elements in the UI.
 * 
 * @constant {string}
 * @example
 * import merchantModelPath from './merchant_model';
 * <img src={merchantModelPath} alt="Merchant Model" />
 */
declare const merchantModelAssetPath: string;

export default merchantModelAssetPath;
/**
 * SVG asset module that exports the path to the upload success icon.
 * This module provides access to a static SVG asset used to indicate successful upload operations.
 * 
 * @module UploadSuccessAsset
 */

/**
 * The public path to the upload success SVG icon asset.
 * This path is resolved at build time and points to the optimized SVG file in the assets directory.
 * 
 * @example
 *
/**
 * Asset module that exports the resolved path to the jiyoujia image.
 * This module represents a static asset import in the webpack bundle.
 */

/**
 * The resolved public path to the jiyoujia image asset.
 * Typically resolves to something like "/assets/jiyoujia.7a107909.png"
 * where the hash (7a107909) is used for cache busting.
 * 
 * @type {string}
 */
declare const jiyoujiaPngPath: string;

export default jiyoujiaPngPath;
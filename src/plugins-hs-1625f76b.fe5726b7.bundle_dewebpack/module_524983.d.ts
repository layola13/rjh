/**
 * Asset module export for user avatar SVG icon.
 * 
 * This module exports the URL path to a static SVG asset that represents
 * a user icon or avatar image. The asset is processed by the build system
 * and includes a content hash in the filename for cache busting.
 * 
 * @module UserAsset
 */

/**
 * The public URL path to the user SVG icon asset.
 * The path includes a content hash (a8f7b2c3) for browser cache invalidation.
 * 
 * @example
 *
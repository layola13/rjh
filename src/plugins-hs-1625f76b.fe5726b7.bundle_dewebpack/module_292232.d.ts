/**
 * Asset module export for replace icon SVG
 * 
 * This module exports the URL path to the replace icon SVG asset.
 * The asset is processed by the build system and typically includes a content hash
 * for cache busting purposes.
 * 
 * @module ReplaceIconAsset
 */

/**
 * URL path to the replace icon SVG asset
 * 
 * The path is relative to the application's public base URL and includes
 * a content hash (d0a18128) for long-term caching and cache invalidation.
 * 
 * @example
 *
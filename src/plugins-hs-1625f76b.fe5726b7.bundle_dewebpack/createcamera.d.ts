/**
 * Camera management module
 * Provides functions to create and delete camera instances
 */

/**
 * Creates a new camera instance
 * @returns {unknown} The newly created camera object
 */
export function CreateCamera(): unknown;

/**
 * Deletes an existing camera instance
 * @param camera - The camera instance to be deleted
 * @returns {unknown} Result of the deletion operation
 */
export function DeleteCamera(camera: unknown): unknown;
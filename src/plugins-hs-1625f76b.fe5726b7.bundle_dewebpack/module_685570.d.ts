/**
 * Asset module that exports the path to the report close icon SVG file.
 * This is typically used in UI components that need to display a close button for reports.
 * 
 * @module ReportCloseAsset
 */

/**
 * The public path to the report close icon SVG asset.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @example
 *
/**
 * SVG asset module declaration
 * 
 * This module exports the public path to a hover state SVG icon for model application entry.
 * The asset is processed by the build system and the final path includes a content hash
 * for cache busting.
 * 
 * @module ModelApplyEntryHoverAsset
 */

/**
 * The public URL path to the model apply entry hover SVG asset.
 * 
 * The path includes a content hash (b32b06eb) generated during the build process
 * to ensure proper cache invalidation when the asset content changes.
 * 
 * @example
 *
/**
 * CSS Module Type Definitions
 * 
 * This module represents a CSS Modules import that exports:
 * 1. CSS class name mappings (locals)
 * 2. Re-exported utilities from the underlying CSS loader
 */

/**
 * CSS class name mappings exported by the CSS module.
 * Maps authored class names to their hashed/scoped equivalents.
 * 
 * @example
 *
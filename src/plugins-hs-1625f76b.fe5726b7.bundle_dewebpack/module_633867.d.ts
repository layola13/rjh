/**
 * Asset module that exports the path to the material replace enter SVG icon.
 * This module provides a reference to a static SVG asset used in the application's UI.
 * 
 * @module MaterialReplaceEnterAsset
 */

/**
 * The file path to the material replace enter SVG icon asset.
 * This path is typically resolved at build time and points to the bundled asset location.
 * 
 * @constant {string} MATERIAL_REPLACE_ENTER_ICON_PATH
 * @default "assets/materialreplaceenter.38376639.svg"
 */
declare const MATERIAL_REPLACE_ENTER_ICON_PATH: string;

export default MATERIAL_REPLACE_ENTER_ICON_PATH;
/**
 * Asset module that exports the path to the lightslot_up SVG image.
 * This is typically used in applications that need to reference static assets
 * with cache-busted filenames.
 */

/**
 * The public path to the lightslot_up SVG asset.
 * The filename includes a content hash (96423acd) for cache busting.
 * 
 * @example
 *
/**
 * Asset module export for lock icon SVG
 * Provides the public URL path to the lock.svg asset
 */

/**
 * The public URL path to the lock icon SVG asset.
 * This path is resolved at build time and points to the hashed asset file.
 * 
 * @example
 *
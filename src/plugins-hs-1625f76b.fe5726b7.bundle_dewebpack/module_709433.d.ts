/**
 * Asset module exporting the path to the styler image search PNG file.
 * This module provides the resolved URL for the static image asset after webpack processing.
 * 
 * @module assets/stylerimagesearch
 */

/**
 * The fully resolved public path to the styler image search asset.
 * This path includes the configured public path prefix and the content-hashed filename.
 * 
 * @example
 *
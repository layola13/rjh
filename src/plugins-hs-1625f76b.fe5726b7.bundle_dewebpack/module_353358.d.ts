/**
 * 数组工具函数模块
 * 提供数组差集和交集的计算功能
 */

/**
 * 计算两个数组的差集（存在于第一个数组但不存在于第二个数组的元素）
 * @template T - 数组元素的类型
 * @param firstArray - 第一个数组
 * @param secondArray - 第二个数组
 * @returns 返回差集数组，包含所有在 firstArray 中但不在 secondArray 中的元素
 * @example
 *
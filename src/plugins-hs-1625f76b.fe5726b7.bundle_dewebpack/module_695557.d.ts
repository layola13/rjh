/**
 * Asset module that exports the path to the lock dimension normal SVG icon.
 * This module provides access to a static asset (SVG file) that represents
 * a normal state lock dimension icon, commonly used in UI for dimension locking features.
 * 
 * @module LockDimensionNormalAsset
 */

/**
 * The relative or absolute URL path to the lock_dimension_normal.svg asset.
 * This path is resolved at build time and includes content hash for cache busting.
 * 
 * Format: "assets/lock_dimension_normal.[hash].svg"
 * 
 * @example
 *
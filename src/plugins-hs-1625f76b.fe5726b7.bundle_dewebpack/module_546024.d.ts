/**
 * Asset module that exports the path to the satisfaction survey model SVG image.
 * This module provides access to a static asset file used in the application's UI.
 * 
 * @module SatisfactionSurveyModel
 */

/**
 * The public path to the satisfaction survey model SVG asset.
 * This path is resolved at build time and includes cache-busting hash.
 * 
 * @constant
 * @type {string}
 * @example
 * // Usage in React component:
 * import satisfactionSurveyModelPath from './satisfaction-survey-model';
 * <img src={satisfactionSurveyModelPath} alt="Satisfaction Survey" />
 */
declare const satisfactionSurveyModelPath: string;

export default satisfactionSurveyModelPath;
/**
 * Asset module exporting the path to the 'select_no' SVG icon.
 * This module provides access to a static SVG asset used in the application's UI.
 * 
 * @module SelectNoIcon
 */

/**
 * The public path to the select_no.svg icon asset.
 * This path is resolved at build time and points to the hashed asset file.
 * 
 * @example
 *
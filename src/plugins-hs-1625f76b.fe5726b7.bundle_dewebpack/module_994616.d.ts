/**
 * Asset module that exports the path to the edit slab selected SVG icon.
 * This module represents a statically imported SVG asset that has been processed
 * by the build system and placed in the assets directory with a content hash.
 */

/**
 * The resolved URL path to the edit slab selected icon SVG file.
 * The path includes a content hash (ff6da700) for cache busting purposes.
 * 
 * @example
 *
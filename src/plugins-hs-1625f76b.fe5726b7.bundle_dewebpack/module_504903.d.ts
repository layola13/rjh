/**
 * Asset module that exports the URL path to the ungroup SVG icon.
 * This module is typically used by bundlers to resolve static asset references.
 */

/**
 * The URL path to the ungroup.svg icon asset.
 * Points to the bundled SVG file in the assets directory.
 * 
 * @example
 *
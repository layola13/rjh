/**
 * Asset path declaration for the unlock dimension disabled icon.
 * This module exports the public path to the SVG asset used to represent
 * a disabled or unavailable unlock dimension state in the UI.
 * 
 * @module UnlockDimensionDisableAsset
 */

/**
 * The public URL path to the unlock_dimension_disable SVG asset.
 * This path is resolved at build time and points to the hashed asset file
 * in the production build output.
 * 
 * @example
 *
/**
 * SVG asset module export
 * 
 * This module exports the URL path to the "model_old_active_fp" SVG asset.
 * The asset is typically processed by the build system and includes a content hash
 * for cache busting purposes.
 * 
 * @module AssetExports
 */

/**
 * URL path to the model_old_active_fp SVG asset.
 * The path includes a content hash (edc6c6df) for cache invalidation.
 * 
 * @example
 *
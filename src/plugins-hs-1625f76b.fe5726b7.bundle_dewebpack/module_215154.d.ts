/**
 * Asset module exporting the URL path to the wallflip SVG image.
 * This module represents a static asset import that Webpack has processed
 * and placed in the assets directory with a content hash for cache busting.
 */

/**
 * The public URL path to the wallflip.svg asset.
 * The hash (834830a0) in the filename ensures proper cache invalidation
 * when the asset content changes.
 * 
 * @example
 *
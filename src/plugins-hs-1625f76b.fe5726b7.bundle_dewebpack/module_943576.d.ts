/**
 * Asset module that exports the path to the original material texture SVG file.
 * This module is typically used to reference static assets in a webpack-bundled application.
 * 
 * @module OriginalMaterialTexture
 */

/**
 * The public URL path to the original material texture SVG asset.
 * This path is resolved at build time by the bundler and includes cache-busting hash.
 * 
 * @example
 *
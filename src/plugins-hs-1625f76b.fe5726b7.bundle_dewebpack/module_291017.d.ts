/**
 * Asset module export for large view icon SVG.
 * 
 * This module exports the URL path to the large view icon (normal state, version 2).
 * The SVG asset is served from the assets directory and is typically used in UI components
 * that require a large view icon representation.
 * 
 * @module LargeViewIconAsset
 */

/**
 * The URL path to the large view icon SVG asset.
 * Points to the hashed SVG file in the assets directory.
 * 
 * @example
 *
/**
 * Asset module that exports the URL path to the model activity image.
 * This module is typically used to import and reference static image assets in the application.
 * 
 * @module ModelActivityAsset
 * @example
 * import modelActivityImage from './modelActivity';
 * // modelActivityImage === '/assets/modelActivity.28e8cd38.png'
 */

/**
 * The public URL path to the model activity PNG image asset.
 * The hash in the filename (28e8cd38) is used for cache busting.
 */
declare const modelActivityAssetUrl: string;

export default modelActivityAssetUrl;
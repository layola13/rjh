/**
 * Asset module that exports the file path for the left normal axial SVG image.
 * This is typically used in medical imaging or anatomical visualization contexts.
 * 
 * @module LeftNormalAxialAsset
 */

/**
 * The file path to the left normal axial SVG asset.
 * Points to a static asset served from the application's asset directory.
 */
declare const leftNormalAxialAssetPath: string;

export default leftNormalAxialAssetPath;
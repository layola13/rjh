/**
 * Asset module that exports the path to an SVG file.
 * This module provides the URL/path to the 'model_old_fp' SVG asset.
 * 
 * @module AssetPath
 */

/**
 * The file path or URL to the model_old_fp SVG asset.
 * This path is typically resolved by the build system and includes
 * the hashed filename for cache-busting purposes.
 * 
 * @type {string}
 * @constant
 * @example
 * // Usage in a component
 * import modelIcon from './assets/model_old_fp.svg';
 * // modelIcon will be "assets/model_old_fp.820dcd00.svg"
 */
declare const assetPath: string;

export default assetPath;
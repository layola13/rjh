/**
 * Asset module that exports the path to the large view icon (hover state) SVG asset.
 * This module provides the URL path to the largeviewicon_v2_hover SVG file,
 * which is typically used for displaying a large view icon in its hover state.
 * 
 * @module LargeViewIconV2Hover
 */

/**
 * The URL path to the large view icon hover state SVG asset.
 * The path is resolved relative to the public path configured in the build system.
 * 
 * @example
 *
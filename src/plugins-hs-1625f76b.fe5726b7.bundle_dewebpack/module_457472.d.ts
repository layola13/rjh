/**
 * 基础类模块
 * 提供初始化和反初始化方法的基类
 */

/**
 * 基础类，提供生命周期管理方法
 * @remarks
 * 此类实现了基本的初始化和反初始化模式，可用作需要显式资源管理的对象的基类
 */
export default class BaseLifecycleManager {
  /**
   * 初始化方法
   * @param e - 第一个初始化参数（具体类型需根据实际使用场景定义）
   * @param t - 第二个初始化参数（具体类型需根据实际使用场景定义）
   * @param n - 第三个初始化参数（具体类型需根据实际使用场景定义）
   * @remarks
   * 用于设置对象的初始状态或分配资源
   * 下划线后缀通常表示这是一个受保护的内部方法
   */
  init_(e: unknown, t: unknown, n: unknown): void;

  /**
   * 反初始化方法
   * @remarks
   * 用于清理资源、重置状态或执行清理操作
   * 应在对象生命周期结束时调用
   */
  uninit_(): void;
}
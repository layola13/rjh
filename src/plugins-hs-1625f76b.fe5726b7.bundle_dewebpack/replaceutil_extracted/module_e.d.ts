/**
 * Throws the provided error or value.
 * This is a utility function that unconditionally throws whatever is passed to it.
 * Commonly used in error handling pipelines or as a rejection handler.
 * 
 * @param error - The error or value to throw
 * @throws Always throws the provided error parameter
 * @returns Never returns (always throws)
 * 
 * @example
 *
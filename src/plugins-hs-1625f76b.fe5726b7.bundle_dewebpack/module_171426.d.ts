/**
 * Asset module for lineToCurvedwall SVG image.
 * 
 * This module exports the path to a static SVG asset that is processed
 * by the bundler and made available at runtime.
 * 
 * @module LineToCurvedwallAsset
 */

/**
 * The public URL path to the lineToCurvedwall SVG asset.
 * 
 * This path is resolved at build time and includes the content hash
 * for cache busting (f971bcc7).
 * 
 * @example
 *
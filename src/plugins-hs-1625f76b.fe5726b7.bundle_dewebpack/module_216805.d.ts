/**
 * Webpack asset module that exports the URL path to a zoom-in icon SVG file.
 * This module represents a static asset reference typically used in UI components
 * for zoom/magnification functionality.
 * 
 * @module ZoomInAsset
 */

/**
 * The URL path to the zoom-in SVG icon asset.
 * This path is resolved at build time by the bundler and points to the
 * hashed asset file in the output directory.
 * 
 * @example
 *
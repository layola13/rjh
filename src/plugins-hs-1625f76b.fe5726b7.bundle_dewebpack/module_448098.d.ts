/**
 * Asset module that exports the path to a UI XML configuration file.
 * This module provides the runtime path to the ui.xml asset after build processing.
 * 
 * @module UIAssetPath
 */

/**
 * The resolved path to the UI XML asset file.
 * This path is dynamically generated during the build process and includes
 * content hash for cache busting (c1908a0b).
 * 
 * @type {string}
 * @example
 *
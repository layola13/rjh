/**
 * Asset module that exports the file path for the settings background SVG.
 * This module provides access to a static asset processed by the build system.
 */

/**
 * The public URL path to the settings background SVG asset.
 * This path is resolved at build time and includes content hash for cache busting.
 * 
 * @example
 *
/**
 * CSS Module type definitions
 * 
 * This module represents a CSS loader configuration for webpack,
 * typically used for importing CSS modules with type-safe class names.
 */

/**
 * CSS Module class names mapping
 * 
 * An object containing CSS class names as keys and their hashed/scoped 
 * equivalents as values, enabling type-safe access to CSS module classes.
 * 
 * @example
 *
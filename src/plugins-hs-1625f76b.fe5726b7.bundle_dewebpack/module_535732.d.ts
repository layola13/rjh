/**
 * Asset module export for Rome Type SVG
 * 
 * This module exports the public URL path to a static SVG asset.
 * The asset is processed by the build system and given a content-hashed filename
 * for cache busting purposes.
 * 
 * @module RomeTypeSVG
 */

/**
 * Public URL path to the Rome Type SVG asset
 * 
 * The actual filename includes a content hash (c24e0f69) to ensure
 * proper cache invalidation when the file content changes.
 * 
 * @example
 *
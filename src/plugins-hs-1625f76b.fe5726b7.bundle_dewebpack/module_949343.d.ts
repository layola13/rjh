/**
 * Asset module that exports the path to the "lightslot_up_selected" SVG icon.
 * This asset is used for representing a selected upward-facing light slot in the UI.
 * 
 * @module LightSlotUpSelectedAsset
 */

/**
 * The public path to the lightslot_up_selected SVG asset.
 * This path is resolved at build time and points to the static asset location.
 * 
 * @example
 *
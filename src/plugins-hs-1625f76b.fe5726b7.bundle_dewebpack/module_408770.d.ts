/**
 * Asset module that exports the path to the lock dimension hover SVG icon.
 * This asset is typically used in UI components to display a hover state
 * for dimension locking functionality.
 * 
 * @module LockDimensionHoverAsset
 */

/**
 * The public path to the lock dimension hover SVG asset.
 * Resolved at build time to include the correct base URL and hashed filename.
 * 
 * @example
 *
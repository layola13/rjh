/**
 * Asset module export for group2 SVG image
 * This module provides the URL path to a static SVG asset that is processed during the build.
 * The asset is typically used for UI components requiring the group2 graphic.
 */

/**
 * The public URL path to the group2.svg asset
 * @remarks
 * This path is resolved at build time by the bundler and points to the
 * hashed asset file in the assets directory (e.g., "assets/group2.7e7bc8be.svg")
 */
declare const assetUrl: string;

export default assetUrl;
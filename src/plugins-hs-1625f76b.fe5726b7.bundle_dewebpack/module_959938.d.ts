/**
 * Asset module that exports the path to the model brand image.
 * This module represents a static asset (PNG image) that has been processed
 * by the build system and is available at a hashed path for cache busting.
 */

/**
 * The relative or absolute URL path to the model brand PNG image asset.
 * The path includes a content hash (ae485791) for optimal browser caching.
 * 
 * @example
 *
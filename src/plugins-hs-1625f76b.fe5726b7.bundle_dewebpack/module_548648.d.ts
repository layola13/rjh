/**
 * CSS Module type definitions
 * 
 * This module represents a CSS Module loader that injects styles into the DOM
 * and exports CSS class name mappings for use in TypeScript/JavaScript code.
 */

/**
 * CSS class name mappings exported by the CSS Module.
 * Each key corresponds to a class name defined in the CSS file,
 * and the value is the transformed/hashed class name used at runtime.
 * 
 * @example
 *
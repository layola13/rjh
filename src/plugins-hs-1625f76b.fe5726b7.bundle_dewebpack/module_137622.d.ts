/**
 * Asset module for noShop SVG icon
 * 
 * This module exports the URL path to the noShop.svg asset.
 * The asset is processed by the build system and includes a content hash
 * for cache busting (fa155950).
 * 
 * @module NoShopAsset
 */

/**
 * URL path to the noShop SVG asset
 * 
 * This path is resolved at build time by the bundler and includes
 * the public path prefix and content hash for long-term caching.
 * 
 * @example
 *
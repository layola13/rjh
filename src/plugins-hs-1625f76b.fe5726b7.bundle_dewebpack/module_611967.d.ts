/**
 * Asset module that exports the path to the lightband not-flip selected SVG icon.
 * This module is typically used by bundlers to resolve static asset references.
 * 
 * @module LightbandNotFlipSelectedAsset
 */

/**
 * The resolved public path to the lightband not-flip selected SVG asset.
 * This path is generated at build time and points to the static asset location.
 * 
 * @example
 *
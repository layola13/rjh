/**
 * Extracts and returns the full year from a Date object as a string.
 * 
 * @param date - The Date object to extract the year from
 * @returns The full year as a string (e.g., "2024")
 * 
 * @example
 *
/**
 * Asset module export for SVG file
 * 
 * This module exports the public URL path to a static SVG asset.
 * The asset is processed by the build system and includes a content hash
 * in the filename for cache busting.
 * 
 * @module TianjiaHoverIcon
 */

/**
 * Public URL path to the "tianjia_hover" SVG icon asset.
 * 
 * The path is relative to the application's public asset directory
 * and includes a content hash (70667e04) for browser cache invalidation.
 * 
 * @example
 *
/**
 * Asset module declaration for column_round SVG file.
 * This module exports the public path to the SVG asset bundled by the build tool.
 * 
 * @module column_round
 */

/**
 * The public URL path to the column_round.svg asset.
 * This path is resolved at build time and points to the versioned asset
 * in the assets directory (e.g., "assets/column_round.75b37254.svg").
 * 
 * @type {string}
 */
declare const columnRoundAssetPath: string;

export default columnRoundAssetPath;
/**
 * Module: AddSplitPointRequest
 * 
 * 提供用于向二维草图添加分割点的请求类。
 * 该类继承自 HSApp.ExtraordinarySketch2d.Request.AddSplitPointRequest
 */

import { HSApp } from './HSApp';

/**
 * 添加分割点请求类
 * 
 * 用于在二维草图中添加分割点的请求对象。
 * 继承自 HSApp.ExtraordinarySketch2d.Request.AddSplitPointRequest 基类。
 * 
 * @extends HSApp.ExtraordinarySketch2d.Request.AddSplitPointRequest
 * 
 * @example
 *
/**
 * Sets the flag to ignore mouse events.
 * 
 * @param shouldIgnore - Whether to ignore mouse events or not
 */
function setIgnoreMouseEvent(shouldIgnore: boolean): void {
    this._ignoreMouseEvent = shouldIgnore;
}
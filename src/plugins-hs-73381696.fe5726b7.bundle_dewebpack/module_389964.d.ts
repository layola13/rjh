/**
 * Asset module for delete vertex icon
 * 
 * This module exports the URL path to the delete vertex SVG icon asset.
 * The asset is managed by the build system and includes a content hash for cache busting.
 * 
 * @module DeleteVertexAsset
 */

/**
 * The public URL path to the delete vertex SVG icon.
 * 
 * This path is resolved at build time and includes a content hash (ec8757f7)
 * to ensure proper cache invalidation when the asset changes.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the copywall SVG icon.
 * This module provides a reference to a static SVG asset used in the application UI.
 * 
 * @module assets/copywall
 */

/**
 * The public URL path to the copywall.svg asset.
 * This path is resolved at build time and points to the optimized SVG file
 * with content hash for cache busting.
 * 
 * @example
 *
/**
 * Asset module exporting the URL path to the sewer pipe square SVG image.
 * This module provides access to a static SVG asset that is processed and hashed
 * during the build process for cache-busting purposes.
 * 
 * @module AssetPaths
 */

/**
 * The URL path to the sewer pipe square SVG asset.
 * The filename includes a content hash (efbf0111) for cache invalidation.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the finished hover state SVG icon.
 * This asset is typically used to display a hover state for completed/finished items in the UI.
 * 
 * @module FinishedHoverAsset
 */

/**
 * The public path to the finished hover state SVG asset.
 * Resolved at build time by the bundler to the correct asset URL.
 * 
 * @example
 *
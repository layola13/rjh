/**
 * Webpack asset module that exports the URL path to the settings SVG icon.
 * This module represents a static asset reference that would be processed by Webpack's asset/resource loader.
 * 
 * @module SettingsIcon
 */

/**
 * The public URL path to the settings icon SVG file.
 * This path is resolved by the bundler and points to the hashed asset in the output directory.
 * 
 * @example
 *
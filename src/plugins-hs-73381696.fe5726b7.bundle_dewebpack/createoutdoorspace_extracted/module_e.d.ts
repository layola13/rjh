/**
 * Error handler module that throws any error passed to it.
 * This is typically used as a fallback or default error handler in module loading systems.
 */

/**
 * Throws the provided error, propagating it up the call stack.
 * 
 * @param error - The error object to be thrown
 * @throws Always throws the provided error
 * @example
 *
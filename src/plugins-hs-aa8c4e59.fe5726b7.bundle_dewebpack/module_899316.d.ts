/**
 * SVG asset module for cancel icon
 * Exports the URL path to the cancel.svg asset
 * @module cancel-icon
 */

/**
 * URL path to the cancel icon SVG asset
 * The asset is processed by the build system and the path includes a content hash
 * for cache busting (ea3984bd)
 */
declare const cancelIconPath: string;

export default cancelIconPath;
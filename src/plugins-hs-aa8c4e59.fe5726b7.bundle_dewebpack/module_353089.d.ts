/**
 * CSS module export for AIDA entry component styles
 * Defines positioning and z-index for a floating entry point UI element
 */

/**
 * CSS class name for the AIDA entry component
 * 
 * Styles applied:
 * - Positioned absolutely at bottom-right of container
 * - Fixed dimensions: 50px height
 * - High z-index (1100) to ensure visibility above other content
 * - Pointer cursor for interactivity
 */
export const aidaEntry: string;

/**
 * Default export containing all CSS module class names
 */
declare const styles: {
  readonly 'aida-entry': string;
};

export default styles;
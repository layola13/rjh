/**
 * Asset module that exports the path to a global tip image.
 * This module provides a reference to a static JPEG asset used for displaying global tips.
 * 
 * @module AssetTipGlobal
 */

/**
 * The public path to the global tip image asset.
 * This path is resolved at build time and points to the optimized image file.
 * 
 * @example
 *
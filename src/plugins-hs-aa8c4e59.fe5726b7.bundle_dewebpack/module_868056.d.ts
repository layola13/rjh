/**
 * Asset module export for pin icon SVG file
 * 
 * This module provides the public URL path to the pin icon asset.
 * The asset is typically used in map markers, location indicators, or UI pinning features.
 * 
 * @module PinAsset
 */

/**
 * Public URL path to the pin SVG icon asset
 * 
 * The path is resolved by the build system and points to the static asset location.
 * In production, this typically resolves to a CDN or static file server path.
 * 
 * @example
 *
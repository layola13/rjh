/**
 * Asset module declaration for delete icon SVG
 * 
 * This module exports the URL path to a statically imported SVG asset.
 * The actual file path is resolved at build time by the bundler.
 * 
 * @module DeleteIconAsset
 */

/**
 * The resolved public URL path to the delete icon SVG asset.
 * 
 * This string contains the full public path to the delete.svg file,
 * including any CDN prefix, public path, or hash for cache busting.
 * 
 * @example
 *
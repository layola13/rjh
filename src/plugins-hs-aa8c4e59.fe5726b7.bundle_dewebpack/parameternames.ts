export enum ParameterNames {
  parentId = "parentId",
  center = "center",
  rotation = "rotation",
  scale = "scale",
  size = "size",
  path = "path",
  area = "area",
  roomId = "roomId",
  layerId = "layerId"
}
/**
 * Asset module export for delete background SVG image.
 * This module provides the path to a static SVG asset used for delete button backgrounds.
 * 
 * @module DeleteBackgroundAsset
 */

/**
 * The public path to the delete background SVG asset.
 * This path is resolved at build time and points to the optimized SVG file.
 * 
 * @constant
 * @type {string}
 * @example
 *
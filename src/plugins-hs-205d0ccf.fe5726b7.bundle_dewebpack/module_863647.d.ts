/**
 * Pagination component props type definition
 * Re-exported from the base pagination component library
 */
export type { PaginationProps } from './base-pagination';

/**
 * Custom pagination component for teaching interface
 * 
 * @remarks
 * This component wraps the base Pagination component with teaching-specific styling and defaults.
 * It applies theme-aware class names and sets simplified, small-sized pagination controls.
 * 
 * @param props - Pagination configuration options
 * @returns A React element rendering a themed pagination component
 * 
 * @example
 *
/**
 * Asset module that exports the path to the wallflip SVG image.
 * This module represents a static asset reference typically handled by bundlers.
 * 
 * @module wallflip-asset
 */

/**
 * The public path to the wallflip SVG asset.
 * This path is resolved at build time and points to the static asset location.
 * 
 * @example
 *
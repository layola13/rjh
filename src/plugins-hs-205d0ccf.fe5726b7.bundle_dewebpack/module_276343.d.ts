/**
 * Asset path declaration for the add boundary SVG icon.
 * This module exports the webpack-bundled path to the add_boundary.svg asset.
 * 
 * @module AssetPath
 */

/**
 * The resolved path to the add_boundary.svg asset.
 * This path is constructed by webpack during the build process and includes
 * the content hash (3b4193ca) for cache busting purposes.
 * 
 * @example
 *
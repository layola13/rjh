/**
 * 计算裁剪窗口的尺寸和位置
 * 
 * 该函数根据给定的宽高比和当前窗口尺寸，计算出居中的裁剪窗口的坐标和尺寸。
 * 窗口高度会减去82像素的固定偏移量（通常用于头部或工具栏高度）。
 * 裁剪区域高度为可用高度的70%。
 * 
 * @param aspectRatio - 裁剪窗口的宽高比（宽度/高度）
 * @returns 包含4个元素的元组：
 *   - [0]: 裁剪窗口的宽度（像素）
 *   - [1]: 裁剪窗口的高度（像素）
 *   - [2]: 裁剪窗口相对于浏览器窗口左边缘的X坐标（像素）
 *   - [3]: 裁剪窗口相对于浏览器窗口顶部的Y坐标（像素）
 * 
 * @example
 *
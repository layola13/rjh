/**
 * Asset module that exports the path to the back01 SVG image.
 * This module provides access to a static asset (SVG file) that is managed by the build system.
 * 
 * @module AssetPath
 * @returns The public URL path to the back01.svg asset
 */

/**
 * The public URL path to the back01 SVG asset.
 * This path is resolved by the bundler and points to the hashed asset file.
 * 
 * @example
 *
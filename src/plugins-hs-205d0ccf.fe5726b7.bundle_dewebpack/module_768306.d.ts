/**
 * Asset module export for crop region SVG
 * 
 * This module exports the public path to the crop_region SVG asset.
 * The asset is typically used for displaying crop region UI elements.
 * 
 * @module CropRegionAsset
 */

/**
 * Public URL path to the crop region SVG asset
 * 
 * @remarks
 * This path is resolved at build time and points to the static asset
 * in the application's public directory. The hash in the filename
 * (67372876) ensures proper cache busting.
 * 
 * @example
 *
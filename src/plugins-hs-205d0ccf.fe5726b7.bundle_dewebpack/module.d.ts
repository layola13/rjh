/**
 * Editor DOM query utility function
 * 
 * @description
 * Provides a convenient way to query DOM elements within the #editor container.
 * When a selector is provided, it searches within #editor. Otherwise, returns the #editor element itself.
 * 
 * @param selector - Optional CSS selector string to find elements within the editor
 * @returns jQuery object containing the matched element(s)
 * 
 * @example
 *
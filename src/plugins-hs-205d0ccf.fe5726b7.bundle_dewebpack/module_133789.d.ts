/**
 * Webpack asset module that exports the URL path to the toolbar import SVG icon.
 * This module provides access to the static asset after build-time processing.
 * 
 * @module ToolbarImportAsset
 */

/**
 * The public URL path to the toolbar import SVG icon asset.
 * This path is resolved by the bundler and points to the hashed asset file.
 * 
 * @example
 *
/**
 * Asset path resolver module
 * Exports the public path to the UI XML asset file
 */

/**
 * The public URL path to the UI XML asset file.
 * This path is resolved at build time and points to the compiled asset.
 * 
 * @example
 *
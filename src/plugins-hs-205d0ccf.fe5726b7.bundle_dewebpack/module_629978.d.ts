/**
 * Asset module that exports the path to the edit customized type SVG icon.
 * This module provides a static reference to the SVG asset used for edit customization UI elements.
 * 
 * @module EditCustomizedTypeAsset
 */

/**
 * The resolved public path to the edit customized type SVG icon asset.
 * This path is computed at build time and points to the bundled SVG file.
 * 
 * @example
 *
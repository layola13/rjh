/**
 * Cancel icon SVG asset module
 * 
 * This module exports the public path to the cancel icon SVG file.
 * The asset is processed by the build system and includes a content hash
 * for cache busting purposes.
 * 
 * @module CancelIconAsset
 */

/**
 * The public URL path to the cancel.svg icon asset.
 * 
 * @remarks
 * This path is generated at build time and includes a content hash (837300dc)
 * to ensure proper cache invalidation when the file content changes.
 * 
 * @example
 *
/**
 * Webpack asset module export
 * 
 * This module represents a static asset (PNG image) that was processed by Webpack's asset module system.
 * The original file is 'bg_lingtu_tuji.png' which has been hashed and output to the assets directory.
 * 
 * @module AssetModule
 */

/**
 * The resolved public path URL for the background image asset.
 * 
 * This path points to the hashed PNG file in the assets directory and can be used
 * directly in img src attributes or CSS background-image properties.
 * 
 * Format: "assets/bg_lingtu_tuji.[contenthash].png"
 * 
 * @example
 *
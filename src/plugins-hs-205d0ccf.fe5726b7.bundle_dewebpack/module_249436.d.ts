/**
 * Asset module that exports the path to the bg_lingtu_3 background image.
 * This module provides a reference to a static PNG asset used for UI backgrounds.
 * 
 * @module AssetPath
 */

/**
 * The full URL path to the bg_lingtu_3.png background image asset.
 * The path is constructed using the public path configuration and includes
 * a content hash (b221bd8f) for cache busting.
 * 
 * @type {string}
 * @example
 * // Usage in a component
 * import bgImagePath from './assets/bg_lingtu_3';
 * <img src={bgImagePath} alt="Background" />
 */
declare const assetPath: string;

export default assetPath;
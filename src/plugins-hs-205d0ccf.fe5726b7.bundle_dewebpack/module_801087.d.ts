/**
 * Asset module that exports the URL path to a group SVG icon.
 * 
 * This module provides access to a static SVG asset used for group-related UI elements.
 * The asset is typically processed by the build system and the path includes a content hash
 * for cache busting purposes.
 * 
 * @module GroupIconAsset
 */

/**
 * The public URL path to the group.svg icon asset.
 * 
 * This path is resolved at build time and includes a content hash (2732f41c)
 * to enable long-term caching. The actual file is located in the assets directory
 * of the built application.
 * 
 * @example
 *
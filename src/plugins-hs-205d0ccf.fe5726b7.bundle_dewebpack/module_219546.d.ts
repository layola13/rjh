/**
 * Asset module that exports the path to the auto-recommend SVG icon.
 * This module provides a reference to a static asset used in the application's UI.
 * 
 * @module AutoRecommendAsset
 */

/**
 * The public URL path to the auto-recommend SVG asset.
 * This path is resolved at build time and points to the hashed static asset.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the cabinet edit SVG icon.
 * This module provides a reference to a static SVG asset used in the application.
 * 
 * @module CabinetEditAsset
 */

/**
 * The public path to the cabinet edit SVG icon asset.
 * This path is resolved at build time and points to the optimized SVG file.
 * 
 * @type {string}
 * @example
 * import cabinetEditIcon from './cabinet-edit-asset';
 * // cabinetEditIcon === "assets/cabinet_edit.ec21bccb.svg"
 */
declare const cabinetEditAssetPath: string;

export default cabinetEditAssetPath;
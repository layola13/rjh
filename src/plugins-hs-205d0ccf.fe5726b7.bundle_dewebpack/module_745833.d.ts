/**
 * Asset module for temproom6 SVG file
 * 
 * This module exports the public path to a static SVG asset.
 * Typically used in build systems to reference bundled static resources.
 */

/**
 * The public URL path to the temproom6 SVG asset.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the lingtu background image.
 * This module represents a webpack asset reference that has been processed
 * and placed in the build output directory.
 * 
 * @module AssetModule_773870
 */

/**
 * The full URL path to the lingtu background image asset.
 * This path includes the public path prefix and content hash for cache busting.
 * 
 * @example
 * // Usage in component
 * import bgLingtuPath from './bg-lingtu-asset';
 * <img src={bgLingtuPath} alt="Lingtu Background" />
 */
declare const assetPath: string;

export default assetPath;
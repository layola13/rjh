/**
 * SVG asset module export
 * 
 * This module exports the public URL path to the edit_cell SVG icon asset.
 * The asset is processed by the build system and the final path includes
 * a content hash for cache busting (ac5eca5f).
 * 
 * @module EditCellAsset
 */

/**
 * Public URL path to the edit cell SVG icon
 * 
 * This path is resolved at build time and points to the optimized
 * SVG asset in the output directory. The hash in the filename ensures
 * proper cache invalidation when the asset changes.
 * 
 * @example
 *
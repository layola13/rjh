/**
 * Asset module for split wall SVG image.
 * This module exports the resolved path to the split_wall.svg asset.
 * 
 * @module split_wall_asset
 */

/**
 * The public path to the split wall SVG asset.
 * This path is resolved at build time and points to the optimized SVG file.
 * 
 * @example
 *
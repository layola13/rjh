/**
 * Asset module that exports the path to a closed wall check icon SVG file.
 * This module provides the resolved public path for the static asset.
 * 
 * @module AssetCheckWallClosed
 */

/**
 * The public path to the closed wall check icon SVG asset.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @constant {string} checkWallClosedAssetPath - Resolved path: "assets/check_wall_closed.c7df1a33.svg"
 */
declare const checkWallClosedAssetPath: string;

export default checkWallClosedAssetPath;
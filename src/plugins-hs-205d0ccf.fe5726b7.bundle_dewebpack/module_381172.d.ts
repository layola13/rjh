/**
 * Webpack asset module that exports the public path to an SVG file.
 * This module provides the URL to the view render result icon asset.
 * 
 * @module AssetModule
 */

/**
 * The public URL path to the view_render_result SVG asset.
 * This path is resolved by the build system and points to the hashed asset file.
 * 
 * @example
 *
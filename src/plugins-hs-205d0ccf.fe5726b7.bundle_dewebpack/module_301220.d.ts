/**
 * SVG asset module that exports the path to the zoom-in toolbar icon.
 * This module provides a reference to a static SVG asset used in the application's UI.
 * 
 * @module ZoomInToolbarIcon
 */

/**
 * The file path to the zoom-in toolbar SVG icon asset.
 * 
 * This path is resolved at build time and points to the static asset location.
 * The asset is typically used for zoom-in functionality in toolbars or UI controls.
 * 
 * @example
 *
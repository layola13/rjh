/**
 * Zoom-out icon asset module
 * 
 * This module exports the URL path to a zoom-out SVG icon asset.
 * The asset is processed by the build system and includes a content hash
 * for cache busting purposes.
 * 
 * @module ZoomOutAsset
 */

/**
 * The public URL path to the zoom-out SVG icon asset.
 * 
 * This path is relative to the application's public directory and includes
 * a content hash (98a769ed) for browser cache invalidation when the file changes.
 * 
 * @example
 *
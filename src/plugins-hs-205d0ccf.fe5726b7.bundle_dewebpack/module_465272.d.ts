/**
 * Asset module for measuring hover icon
 * 
 * This module exports the path to a measuring tool hover state SVG asset.
 * The asset is typically used in UI components that provide measurement functionality.
 * 
 * @module assets/measuring_hover
 */

/**
 * The public path to the measuring hover SVG icon asset.
 * 
 * This path is resolved at build time and points to the optimized SVG file
 * in the assets directory. The file hash (3cc1d2fc) ensures proper cache busting.
 * 
 * @example
 *
/**
 * Asset module export for arrow SVG icon.
 * This module provides the public path to the arrow.svg asset file
 * which is processed and versioned by the build system.
 */

/**
 * Public path to the arrow SVG asset.
 * The hash (be0ccdf4) in the filename ensures cache busting when the asset changes.
 * 
 * @example
 *
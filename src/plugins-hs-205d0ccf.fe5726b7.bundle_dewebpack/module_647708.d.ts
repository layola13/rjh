/**
 * Webpack asset module that exports the URL path to an SVG icon file.
 * This module represents a static asset import that gets processed by Webpack's asset module system.
 * The exported path includes a content hash for cache busting.
 * 
 * @module ExportIconAsset
 */

/**
 * The public URL path to the export icon SVG asset.
 * The path is resolved relative to the application's public path and includes
 * a content hash (8901be48) for efficient browser caching.
 * 
 * @example
 *
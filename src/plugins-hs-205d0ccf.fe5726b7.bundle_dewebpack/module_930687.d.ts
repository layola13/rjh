/**
 * Asset module that exports the file path for the temproom8 SVG asset.
 * This module provides access to a static SVG file that is processed and bundled
 * during the build process, with the final path including a content hash for cache busting.
 * 
 * @module temproom8-asset
 */

/**
 * The public path to the temproom8 SVG asset file.
 * The path includes a content hash (8b66ab00) for optimal browser caching.
 * 
 * @example
 *
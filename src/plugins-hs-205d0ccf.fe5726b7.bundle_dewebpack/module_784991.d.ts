/**
 * SVG asset module for offset_region
 * 
 * This module exports the URL path to the offset_region SVG asset.
 * The asset is processed by the build tool and the exported string
 * contains the final public path to the SVG file.
 * 
 * @module offset_region
 * @example
 *
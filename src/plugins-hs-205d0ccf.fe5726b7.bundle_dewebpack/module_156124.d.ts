/**
 * Asset module that exports the path to the removed house template SVG file.
 * This is typically used in build systems to reference static assets with cache-busting hashes.
 */

/**
 * The public URL path to the removed house template SVG asset.
 * The filename includes a content hash (797d3cc4) for cache busting.
 * 
 * @example
 *
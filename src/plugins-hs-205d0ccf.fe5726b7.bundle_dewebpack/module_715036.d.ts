/**
 * Webpack asset module that exports the URL path to the style brush SVG asset.
 * This module represents a static asset import that gets resolved by the bundler.
 * 
 * @module StyleBrushAsset
 * @description Provides the public URL path to the style_brush SVG icon asset
 */

/**
 * The public URL path to the style brush SVG asset.
 * This path is resolved at build time and includes content hash for cache busting.
 * 
 * @example
 *
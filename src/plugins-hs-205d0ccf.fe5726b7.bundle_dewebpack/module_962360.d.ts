/**
 * Asset module export for left_diy SVG icon
 * 
 * This module exports the URL path to a static SVG asset that is processed
 * during the build pipeline. The asset is typically used for UI components
 * requiring a left-oriented customizable icon.
 * 
 * @module LeftDiyAsset
 */

/**
 * The public URL path to the left_diy.svg asset.
 * 
 * This path is resolved at build time and includes the asset hash for cache busting.
 * The actual file is located in the assets directory with a content-based hash suffix.
 * 
 * @example
 *
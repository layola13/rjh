/**
 * Asset module that exports the path to the merge wall joint SVG icon.
 * This module provides access to a static asset used for representing
 * wall joint merge operations in the UI.
 */

/**
 * The public path to the merge wall joint SVG asset.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @example
 *
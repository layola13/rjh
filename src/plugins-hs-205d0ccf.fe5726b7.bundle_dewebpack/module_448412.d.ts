/**
 * Asset module that exports the path to a UI XML configuration file.
 * This module is typically used by build tools to resolve the correct path
 * to the XML asset after bundling and deployment.
 * 
 * @module UIAssetPath
 */

/**
 * The resolved public path to the UI XML asset file.
 * This path includes the content hash (ad64b158) for cache busting.
 * 
 * @example
 *
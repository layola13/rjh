/**
 * SVG asset module that exports the path to a flip icon.
 * This module provides a reference to a static SVG asset used in the application.
 * 
 * @module assets/flip
 */

/**
 * The file path to the flip SVG icon asset.
 * Points to a hashed SVG file for cache busting purposes.
 * 
 * @constant
 * @type {string}
 * @example
 * import flipIconPath from './assets/flip';
 * <img src={flipIconPath} alt="Flip icon" />
 */
declare const flipIconPath: string;

export default flipIconPath;
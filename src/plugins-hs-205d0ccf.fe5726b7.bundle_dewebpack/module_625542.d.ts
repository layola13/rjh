/**
 * Asset module that exports the path to the property icon SVG file.
 * This is typically used in build systems to reference static assets with cache-busting hashes.
 */

/**
 * The public URL path to the property SVG icon asset.
 * The filename includes a content hash (fd0f4a5e) for cache busting.
 * 
 * @example
 *
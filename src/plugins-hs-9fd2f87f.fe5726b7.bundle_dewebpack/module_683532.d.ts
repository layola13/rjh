/**
 * Asset module that exports the path to the popup move animation GIF.
 * This module is typically used to reference static assets in a bundled application.
 * 
 * @module PopupMoveAsset
 */

/**
 * The file path to the popup move animation GIF asset.
 * This path is resolved relative to the public assets directory.
 * 
 * @remarks
 * The hash in the filename (fab386df) is typically generated during the build process
 * for cache busting purposes.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the right select SVG icon.
 * This is typically used in UI components for selection indicators or dropdown menus.
 * 
 * @module RightSelectAsset
 */

/**
 * The public URL path to the right select SVG icon asset.
 * This path is resolved relative to the application's public asset directory.
 * 
 * @example
 *
/**
 * Webpack asset module that exports the URL path to a temperature picker SVG icon.
 * This module represents a static asset reference used in the application's UI.
 * 
 * @module TempPickerAsset
 */

/**
 * The public URL path to the temperature picker SVG asset.
 * This path is resolved at build time and points to the compiled asset location.
 * 
 * @example
 *
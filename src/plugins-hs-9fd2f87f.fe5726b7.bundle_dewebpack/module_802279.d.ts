/**
 * SVG asset module that exports the path to the mirror cancel icon
 * @module MirrorCancelAsset
 */

/**
 * The file path to the mirror cancel SVG icon asset.
 * This path is resolved by the bundler's public path configuration.
 * 
 * @example
 *
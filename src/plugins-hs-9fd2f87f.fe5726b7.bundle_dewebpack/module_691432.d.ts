/**
 * Asset module that exports the path to the active right arrow SVG icon.
 * This module is typically used by webpack's asset/resource loader to reference static assets.
 * 
 * @module YakouRightActiveIcon
 */

/**
 * The public path to the active right arrow SVG icon asset.
 * This path is resolved at build time and points to the optimized asset location.
 * 
 * @example
 *
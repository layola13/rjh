/**
 * Asset module that exports the path to the default view SVG icon.
 * This module is typically used in UI components to reference the default view graphic.
 * 
 * @module DefaultViewAsset
 */

/**
 * The public path to the default view SVG asset.
 * This path is resolved at build time and points to the static asset location.
 * 
 * @example
 *
/**
 * Asset module that exports the path to a rotation popup GIF image.
 * This module is typically used to display a screen rotation prompt to users.
 * 
 * @module PopupRotateAsset
 */

/**
 * The URL path to the popup rotation GIF asset.
 * This asset is used to indicate to users that they should rotate their device.
 * 
 * The path is resolved relative to the public path configured in the build system.
 * 
 * @example
 *
/**
 * Asset module for yakouleft active state SVG icon
 * 
 * This module exports the URL path to a static SVG asset that represents
 * the active state of a "yakouleft" icon (likely a left-pointing arrow or navigation element).
 * 
 * @module AssetModule
 */

/**
 * The public URL path to the yakouleft active state SVG asset.
 * This path is resolved at build time and includes the content hash for cache busting.
 * 
 * @example
 *
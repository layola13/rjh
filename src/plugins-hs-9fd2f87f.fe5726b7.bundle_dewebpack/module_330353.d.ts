/**
 * Webpack asset module that exports the URL path to a polygon tool SVG icon.
 * This module represents a static asset reference in the application's asset pipeline.
 * 
 * @module PolygonToolAsset
 */

/**
 * The public URL path to the polygon tool SVG asset.
 * This path is resolved at build time and points to the optimized asset location.
 * 
 * Typical usage:
 *
/**
 * Webpack asset module that exports the URL path to a lightband SVG image.
 * This module represents a static asset that has been processed by Webpack's asset pipeline.
 * 
 * @module lightband-asset
 */

/**
 * The public URL path to the lightband SVG asset.
 * This path is computed by Webpack at build time and includes the content hash
 * for cache busting (b949d577).
 * 
 * @example
 *
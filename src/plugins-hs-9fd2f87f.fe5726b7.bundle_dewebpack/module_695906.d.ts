/**
 * CAD Import Tool Asset Path
 * 
 * This module exports the path to the CAD import tool SVG asset.
 * The asset is typically used in UI components that need to display
 * the CAD import tool icon.
 * 
 * @module CadImportToolAsset
 */

/**
 * The public path to the CAD import tool SVG asset.
 * 
 * @remarks
 * This path is resolved at build time and points to the optimized
 * SVG file in the assets directory. The hash (ff503b62) ensures
 * proper cache busting when the asset changes.
 * 
 * @example
 *
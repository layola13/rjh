/**
 * Asset module for 'nocontent' image
 * 
 * This module exports the bundled path to the nocontent.png image asset.
 * The actual path is resolved by the bundler at build time and includes
 * a content hash (66d0cb70) for cache busting.
 * 
 * @module assets/nocontent
 * @example
 *
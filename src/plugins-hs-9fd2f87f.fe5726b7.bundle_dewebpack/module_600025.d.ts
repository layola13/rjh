/**
 * Asset module that exports the path to the UI XML configuration file.
 * This module is typically used by asset loaders or UI frameworks to locate
 * and load the XML definition file for user interface components.
 * 
 * @module UIAssetPath
 */

/**
 * The file path to the UI XML asset.
 * This path is resolved relative to the application's public path and
 * points to the compiled UI definition file with content hash for cache busting.
 * 
 * @example
 *
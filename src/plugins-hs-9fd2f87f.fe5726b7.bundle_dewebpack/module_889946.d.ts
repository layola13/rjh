/**
 * Webpack asset module export
 * Exports the public path to an SVG asset file
 */

/**
 * Public URL path to the unlock icon SVG asset
 * @remarks
 * This path is resolved by the bundler at build time and points to the
 * optimized asset in the output directory (assets/unlock.ac56dd1d.svg)
 */
export const unlockIconPath: string;

export default unlockIconPath;
/**
 * Asset module that exports the resolved URL path for the temperature image.
 * This module represents a static asset reference that would typically be processed
 * by a bundler (like Webpack) to resolve to the final asset path.
 * 
 * @module TemperatureAsset
 */

/**
 * The resolved public URL path to the temperature image asset.
 * 
 * In a production build, this would be the full URL path where the image
 * is hosted, including any CDN prefix or public path configuration.
 * 
 * Format: "assets/temperature.dd38aa97.jpg"
 * - The hash (dd38aa97) is typically added for cache busting
 * 
 * @example
 *
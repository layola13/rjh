/**
 * Static asset module that exports the path to the "show_recent" SVG icon.
 * 
 * @module ShowRecentIcon
 * @description This module provides access to the show_recent.svg asset file.
 * The asset is processed by Webpack and placed in the assets directory with a content hash
 * for cache busting purposes.
 */

/**
 * The relative path to the show_recent SVG icon asset.
 * 
 * @remarks
 * This path includes a content hash (88b97f28) for cache invalidation.
 * The actual file location is determined at build time by Webpack's asset handling.
 * 
 * @example
 *
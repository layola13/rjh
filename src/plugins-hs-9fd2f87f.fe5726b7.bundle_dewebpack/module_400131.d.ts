/**
 * Asset module that exports the path to the m5 PNG image.
 * This module provides a type-safe way to reference the static asset.
 */

/**
 * The relative path to the m5 PNG image asset.
 * Points to the hashed image file in the assets directory.
 * @example "assets/m5.6d565dea.png"
 */
export declare const assetPath: string;

export default assetPath;
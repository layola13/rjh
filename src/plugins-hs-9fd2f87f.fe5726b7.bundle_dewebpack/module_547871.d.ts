/**
 * Webpack asset module that exports the URL path to a group SVG icon.
 * This module represents a static asset reference that gets resolved at build time.
 * 
 * @module GroupIconAsset
 */

/**
 * The public URL path to the group icon SVG asset.
 * This path is resolved by the bundler and points to the optimized asset location.
 * 
 * @example
 *
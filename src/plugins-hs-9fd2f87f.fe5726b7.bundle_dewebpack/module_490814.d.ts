/**
 * Asset module that exports the path to a dialog ceiling SVG image.
 * This module provides a reference to a static SVG asset used for dialog ceiling decorations.
 * 
 * @module DialogCeilingAsset
 */

/**
 * The public path to the dialog ceiling SVG asset.
 * This path is resolved at build time and points to the compiled asset location.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the line tool SVG icon.
 * This module represents a static asset reference typically used in UI components
 * for displaying line drawing tool icons in graphic editors or diagram tools.
 * 
 * @module LineToolAsset
 */

/**
 * The file path to the line tool SVG asset.
 * Points to a versioned SVG file located in the assets directory.
 * The hash in the filename (936eed22) ensures cache busting for updates.
 * 
 * @constant {string}
 */
declare const lineToolAssetPath: string;

export default lineToolAssetPath;
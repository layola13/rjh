/**
 * Webpack asset module that exports the URL path to the draw_polygon SVG icon.
 * This module represents a static asset reference typically used in UI components
 * for displaying polygon drawing functionality.
 * 
 * @module DrawPolygonAsset
 */

/**
 * The URL path to the draw_polygon SVG asset.
 * This path is resolved at build time by the bundler and points to the
 * versioned/hashed asset file in the distribution folder.
 * 
 * @example
 *
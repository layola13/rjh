/**
 * Asset module that exports the path to the fillet tool SVG icon.
 * This module provides a reference to a static asset used in the application's UI.
 * 
 * @module FilletToolAsset
 */

/**
 * The relative or absolute path to the fillet tool SVG asset.
 * This path is resolved at build time and points to the actual location
 * of the SVG file in the compiled assets directory.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the undo icon SVG file.
 * This module provides access to a static asset (undo icon) used in the application UI.
 * 
 * @module UndoAsset
 */

/**
 * The public path to the undo icon SVG asset.
 * This path is resolved at build time and points to the bundled asset location.
 * 
 * @example
 *
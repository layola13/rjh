/**
 * Webpack asset module export
 * Exports the public URL path for a static asset (GIF image)
 */

/**
 * Public path to the popup lightband GIF asset
 * 
 * @remarks
 * This path is resolved by the build system and points to the bundled asset
 * in the output directory. The hash in the filename (92f43579) ensures cache busting.
 * 
 * @example
 *
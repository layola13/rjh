/**
 * Asset module that exports the path to the active material selection icon.
 * This SVG asset is used to indicate the currently selected material in the UI.
 * 
 * @module MaterialSelectActiveIcon
 * @remarks
 * The asset path is resolved at build time and includes a content hash (8d26b87c)
 * for cache busting purposes.
 */

/**
 * The public path to the active material selection SVG icon.
 * 
 * @description
 * Points to the SVG asset file used to visually represent an active/selected
 * material state in the material selection interface.
 * 
 * @example
 *
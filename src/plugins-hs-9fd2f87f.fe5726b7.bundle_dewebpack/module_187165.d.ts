/**
 * Asset module that exports the path to an animated GIF image.
 * This module represents a webpack-bundled static asset reference.
 */

/**
 * The public URL path to the popup line animated GIF asset.
 * This path is resolved at build time and includes the content hash for cache busting.
 * 
 * @example
 *
/**
 * Asset module that exports the URL path to a material design select outline hover icon.
 * This module is typically used for UI components that need to display an outlined
 * select input in its hover state.
 * 
 * @module MaterialSelectOutlineHover
 */

/**
 * The public path URL to the material select outline hover SVG asset.
 * This path is resolved at build time and points to the static asset location.
 * 
 * @example
 *
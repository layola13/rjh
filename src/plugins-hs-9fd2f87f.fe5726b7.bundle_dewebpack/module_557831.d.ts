/**
 * Asset module that exports the path to the popup guideline GIF image.
 * This is typically used by webpack's asset/resource module loader to reference static assets.
 * 
 * @module PopupGuidelineAsset
 */

/**
 * The public path to the popup guideline GIF asset.
 * This path is resolved at build time by the bundler and points to the static asset location.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the floor hover dialog SVG icon.
 * This module provides a reference to a static SVG asset used in the application's UI.
 * 
 * @module DialogFloorHoverAsset
 */

/**
 * The public path to the floor hover dialog SVG asset.
 * This path is resolved at build time and points to the compiled asset location.
 * 
 * @example
 *
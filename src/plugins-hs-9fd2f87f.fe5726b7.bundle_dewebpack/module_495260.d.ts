/**
 * Webpack asset module that exports the path to a lightslot SVG file.
 * This module represents a static asset reference that gets resolved at build time.
 */

/**
 * The public path to the lightslot SVG asset.
 * This path is resolved by the build system and points to the final location
 * of the asset in the dist/build directory.
 * 
 * @example
 *
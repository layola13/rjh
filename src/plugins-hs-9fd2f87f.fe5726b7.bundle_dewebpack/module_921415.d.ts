/**
 * SVG asset module that exports the path to the orthogonality icon.
 * 
 * This module provides access to a static SVG file used for representing
 * orthogonality concepts in the application's UI.
 * 
 * @module OrthogonalityAsset
 */

/**
 * The public path to the orthogonality SVG asset.
 * 
 * This string represents the URL path where the orthogonality.svg file
 * can be accessed in the built application. The path is resolved at build
 * time by the bundler and includes a content hash for cache busting.
 * 
 * @example
 *
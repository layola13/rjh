/**
 * Asset module export for ceiling front face SVG
 * 
 * This module exports the path to a static SVG asset used for rendering
 * the front face of an automatic ceiling component in the application.
 * 
 * @module autceilingfrontface
 */

/**
 * The public URL path to the ceiling front face SVG asset.
 * 
 * This path is resolved at build time and points to the optimized
 * SVG file in the assets directory. The hash (5da2ca60) ensures
 * cache busting when the file content changes.
 * 
 * @example
 *
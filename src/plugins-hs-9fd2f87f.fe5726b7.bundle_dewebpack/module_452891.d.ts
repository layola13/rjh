/**
 * Asset module that exports the path to a height icon SVG file.
 * This module provides a reference to a static SVG asset used for height-related UI elements.
 * 
 * @module HeightAsset
 */

/**
 * The public path to the height icon SVG asset.
 * This path is resolved at build time and points to the compiled asset location.
 * 
 * @type {string}
 * @example
 *
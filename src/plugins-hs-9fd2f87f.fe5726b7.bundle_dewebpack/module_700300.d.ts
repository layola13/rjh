/**
 * Door handle position utilities
 * Provides functions to retrieve door handle configurations for different positions
 */

/**
 * Gets the list of products that have handles positioned on the bottom
 * @returns Array of products with bottom-positioned door handles
 */
export function handleOnBottomDoors(): unknown;

/**
 * Gets the list of products that have handles positioned on the top
 * @returns Array of products with top-positioned door handles
 */
export function handleOnTopDoors(): unknown;
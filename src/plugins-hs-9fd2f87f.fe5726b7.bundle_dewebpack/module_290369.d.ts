/**
 * SVG asset module that exports the path to a dialog icon resource.
 * This module provides the resolved URL for the 'dialog_other' SVG asset.
 * 
 * @module DialogOtherAsset
 * @example
 * import dialogOtherIconPath from './dialog_other_asset';
 * // Use the path in an img tag or as a URL reference
 * <img src={dialogOtherIconPath} alt="Dialog icon" />
 */

/**
 * The resolved public path to the dialog_other SVG asset.
 * Points to: assets/dialog_other.6818e85e.svg
 * 
 * @type {string}
 */
declare const dialogOtherAssetPath: string;

export default dialogOtherAssetPath;
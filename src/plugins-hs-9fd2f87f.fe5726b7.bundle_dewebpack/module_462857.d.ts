/**
 * Asset module that exports the path to the flipx SVG icon.
 * This module represents a static asset reference typically used in bundled applications.
 * 
 * @module FlipXAsset
 */

/**
 * The public path to the flipx SVG asset.
 * This path is resolved at build time and points to the compiled asset location.
 * 
 * @example
 *
/**
 * Asset module that exports the path to the 'right' arrow SVG icon.
 * This module provides the public URL for a right arrow icon used in the UI.
 * 
 * @module RightArrowAsset
 * @example
 * import rightArrowUrl from './right-arrow-asset';
 * // Use in img tag: <img src={rightArrowUrl} alt="Right arrow" />
 */

/**
 * The public URL path to the right arrow SVG asset.
 * Typically resolved by the bundler to include content hash for cache busting.
 * 
 * @constant
 * @example "assets/right.77b40cea.svg"
 */
declare const rightArrowAssetUrl: string;

export default rightArrowAssetUrl;
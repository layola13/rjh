/**
 * Export the native ReferenceError constructor.
 * This module provides access to JavaScript's built-in ReferenceError type,
 * typically used for errors related to referencing non-existent variables.
 */
declare const referenceErrorExport: ReferenceErrorConstructor;

export = referenceErrorExport;
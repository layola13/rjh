/**
 * SVG 图标组件类型声明
 * 该模块导出一个用于渲染空状态插图的 React 函数组件
 */

import { ReactElement } from 'react';

/**
 * 空状态插图组件
 * 
 * 渲染一个复杂的 SVG 插图，通常用于显示空状态、404页面或占位符场景。
 * 该插图包含以下视觉元素：
 * - 圆形背景容器 (251x294)
 * - 装饰性圆点和连接线
 * - 人物角色插图
 * - 紫色圆形图标（右上角）
 * - 绿色植物/装饰元素（底部）
 * 
 * @returns {ReactElement} 返回一个包含完整 SVG 插图的 React 元素
 * 
 * @example
 *
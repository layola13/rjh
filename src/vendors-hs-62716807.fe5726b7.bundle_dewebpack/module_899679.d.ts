/**
 * 绑定函数上下文的工具函数
 * 
 * 该模块提供了一个函数，用于将函数与特定上下文绑定。
 * 它接受一个参数数组，其中第一个元素必须是函数，其余元素作为绑定参数。
 * 
 * @module FunctionBinder
 */

/**
 * 将函数与上下文绑定
 * 
 * @param args - 参数数组，第一个元素必须是要绑定的函数，后续元素为绑定参数
 * @returns 绑定后的函数
 * @throws {TypeError} 当第一个参数不是函数时抛出错误
 * 
 * @example
 *
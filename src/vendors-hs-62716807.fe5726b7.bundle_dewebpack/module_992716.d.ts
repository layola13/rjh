/**
 * Escapes a CSS value string for safe usage in stylesheets.
 * 
 * This utility function ensures that CSS values containing special characters
 * are properly quoted and escaped to prevent syntax errors.
 * 
 * @param value - The CSS value to escape. Can be any type, but only strings are processed.
 * @returns The escaped CSS value string, wrapped in quotes if necessary.
 * 
 * @example
 *
/**
 * Resolves a path by combining a base path with a relative path.
 * If the relative path is absolute, returns the relative path directly.
 * Otherwise, joins the base path and relative path together.
 * 
 * @param basePath - The base path to resolve from
 * @param relativePath - The path to resolve (can be relative or absolute)
 * @returns The resolved absolute path
 * 
 * @example
 *
/**
 * 模块 174156 的类型定义
 * 该模块重新导出了模块 503298 的默认导出
 */

/**
 * 从模块 503298 导入的默认导出
 * 注意：由于缺少原始模块的类型信息，这里使用泛型 unknown
 * 实际使用时应根据具体类型进行替换
 */
declare const defaultExport: unknown;

export default defaultExport;

/**
 * 如果模块 503298 导出的是特定类型，请使用以下模式之一：
 * 
 * 示例 1 - 如果是函数：
 * declare function defaultExport(...args: unknown[]): unknown;
 * 
 * 示例 2 - 如果是类：
 * declare class DefaultExport {
 *   constructor(...args: unknown[]);
 * }
 * 
 * 示例 3 - 如果是对象：
 * interface DefaultExport {
 *   [key: string]: unknown;
 * }
 * 
 * 示例 4 - 如果是React组件：
 * import type { ComponentType } from 'react';
 * declare const defaultExport: ComponentType<Record<string, unknown>>;
 */
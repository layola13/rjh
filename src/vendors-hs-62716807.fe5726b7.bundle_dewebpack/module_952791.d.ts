/**
 * React hook that forces a component re-render.
 * Returns a function that increments an internal counter, triggering a re-render.
 * 
 * @returns A callback function that forces the component to re-render when called
 * 
 * @example
 *
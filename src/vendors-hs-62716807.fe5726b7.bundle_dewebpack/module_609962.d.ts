/**
 * Ant Design Input Component Styles Module
 * 
 * This module exports CSS styles for the Ant Design Input component family,
 * including standard inputs, text areas, input groups, search inputs, and
 * affix wrappers with prefix/suffix support.
 * 
 * @module AntInputStyles
 */

/**
 * CSS module exports for Ant Design Input components
 * 
 * Provides comprehensive styling for:
 * - `.ant-input` - Base input field styles
 * - `.ant-input-affix-wrapper` - Input with prefix/suffix icons
 * - `.ant-input-group` - Grouped input components
 * - `.ant-input-search` - Search input with button
 * - `.ant-input-password` - Password input with visibility toggle
 * - `.ant-input-textarea` - Multi-line text input
 * 
 * Supports variants:
 * - Size variants: `sm` (small), `lg` (large)
 * - States: `disabled`, `focused`, `borderless`
 * - RTL (right-to-left) layout support
 * - Compact grouping mode
 * 
 * @remarks
 * This declaration file represents a CSS module loaded via webpack css-loader.
 * The module ID in the original bundle was 609962.
 */
declare module 'ant-design-input-styles' {
  /**
   * CSS string content containing all Ant Design Input component styles
   * 
   * @example
   *
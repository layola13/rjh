/**
 * Type definitions for Function.prototype.apply polyfill/reference module
 * 
 * This module exports a reference to the native Function.prototype.apply method,
 * which is used to call a function with a given `this` value and arguments provided as an array.
 * 
 * @module FunctionApply
 */

/**
 * Reference to the native Function.prototype.apply method.
 * 
 * The apply() method calls a function with a given `this` value, and `arguments` 
 * provided as an array (or an array-like object).
 * 
 * @example
 *
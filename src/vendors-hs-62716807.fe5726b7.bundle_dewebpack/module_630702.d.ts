/**
 * WebOS Browser Detection Module
 * 
 * Detects if the current browser is webOS-based (LG Smart TV platform)
 * while excluding Chrome-based webOS versions.
 * 
 * @module webos-detector
 * @returns {boolean} True if the browser is a non-Chrome webOS browser, false otherwise
 * 
 * @example
 *
/**
 * Type-safe utility for resolving render prop values.
 * Handles both static values and function-based render props.
 */

/**
 * A value that can be either a static type T or a function that returns T.
 * Commonly used in React render props pattern.
 * 
 * @template T - The type of the resolved value
 * 
 * @example
 *
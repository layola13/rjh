/**
 * Checks if a string contains a forward slash character.
 * 
 * @param path - The string to check for forward slash presence
 * @returns True if the string contains at least one forward slash, false otherwise
 * 
 * @example
 *
/**
 * Re-export module for module_722592
 * 
 * This module serves as a barrel export, re-exporting the default export
 * from module 861826 (mapped from original webpack module ID).
 * 
 * @module module_722592
 * @originalId 722592
 */

/**
 * Re-exported default export from the underlying module (861826).
 * The actual type depends on the implementation of module 861826.
 * 
 * Without access to the source module, this is typed as `unknown`.
 * Replace `unknown` with the concrete type when the source module is known.
 */
declare const _default: unknown;

export default _default;
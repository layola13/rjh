/**
 * Joins a base path with a relative path, handling leading and trailing slashes.
 * 
 * @param basePath - The base path (e.g., "/api", "https://example.com/")
 * @param relativePath - The relative path to append (e.g., "users", "/users")
 * @returns The combined path with normalized slashes
 * 
 * @example
 *
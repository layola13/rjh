/**
 * React hook that subscribes to a global store and returns its current state.
 * Automatically manages subscription lifecycle - subscribes on mount and unsubscribes on unmount.
 * 
 * @returns The current state object from the global store
 * 
 * @example
 *
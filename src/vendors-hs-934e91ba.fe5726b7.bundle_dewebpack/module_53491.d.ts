/**
 * Checks if a value is a non-null primitive (not an object).
 * 
 * This function validates that a value is not NaN (by checking equality with itself)
 * and is not an object type (including functions and null).
 * 
 * @param value - The value to check
 * @returns `true` if the value equals itself (not NaN) and is not an object, otherwise `false`
 * 
 * @example
 *
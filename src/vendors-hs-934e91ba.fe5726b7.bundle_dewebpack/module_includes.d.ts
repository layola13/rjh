/**
 * Module: module_includes
 * Original ID: includes
 * 
 * Checks if the collection contains the specified element.
 * This method is a convenience wrapper around the native `has` method.
 * 
 * @template T - The type of elements in the collection
 * @param element - The element to search for in the collection
 * @returns `true` if the element exists in the collection, `false` otherwise
 * 
 * @example
 *
/**
 * Maps each element of an array to a new value using the provided iteratee function.
 * 
 * @template T - The type of elements in the input array
 * @template R - The type of elements in the output array
 * @param array - The array to iterate over
 * @param iteratee - The function invoked per iteration, receives (value, index, array)
 * @returns A new array of transformed values
 * 
 * @example
 *
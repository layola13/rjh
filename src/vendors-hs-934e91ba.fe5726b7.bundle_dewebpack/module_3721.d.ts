/**
 * Creates an array of a specified length by applying a transform function to each index.
 * 
 * @template T - The type of elements in the resulting array
 * @param length - The desired length of the array to create
 * @param transform - A function that takes an index and returns the value for that position
 * @returns An array of the specified length with transformed values
 * 
 * @example
 *
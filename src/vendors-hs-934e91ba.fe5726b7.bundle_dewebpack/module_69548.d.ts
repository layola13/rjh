/**
 * Invokes a function with a fixed argument value of 5.
 * This appears to be a partial application utility that binds the second parameter.
 */

import { InvokeWithNumber } from './module_530054';

/**
 * Applies a function with the number 5 as the second argument.
 * 
 * @template T - The type of the first parameter for the function
 * @template R - The return type of the function
 * @param fn - A function that accepts a value and a number, returning a result
 * @returns The result of invoking the function with the provided argument and 5
 * 
 * @example
 *
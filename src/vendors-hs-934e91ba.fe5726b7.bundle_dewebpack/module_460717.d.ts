/**
 * 模块：module_460717
 * 
 * 此模块导出一个实用函数，用于将源对象的自有属性复制到目标对象。
 * 该函数是浅拷贝操作的核心实现，类似于 Object.assign 的部分功能。
 * 
 * @module module_460717
 */

/**
 * 将源对象的自有属性（包括符号属性）复制到目标对象
 * 
 * 此函数通常用于对象克隆、合并等场景，它会：
 * 1. 获取源对象的所有自有属性（包括符号属性）
 * 2. 将这些属性复制到目标对象
 * 
 * @template T - 源对象类型
 * @template U - 目标对象类型
 * 
 * @param source - 源对象，其属性将被复制
 * @param target - 目标对象，属性将被复制到此对象
 * 
 * @returns 返回目标对象，包含从源对象复制的所有自有属性
 * 
 * @example
 *
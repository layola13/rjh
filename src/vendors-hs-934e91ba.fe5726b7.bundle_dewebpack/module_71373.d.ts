/**
 * Type guard utility that returns the input if it's a function, otherwise returns a default function.
 * 
 * @template T - The expected function type
 * @param value - The value to check if it's a function
 * @returns The original value if it's a function, otherwise a default/fallback function
 * 
 * @example
 *
/**
 * Gets the size of a collection.
 * 
 * Returns the number of elements in arrays, strings, or array-like objects.
 * For Map and Set objects, returns the size property.
 * For plain objects, returns the number of enumerable own properties.
 * 
 * @param collection - The collection to inspect (array, object, string, Map, Set, etc.)
 * @returns The size of the collection. Returns 0 if the collection is null or undefined.
 * 
 * @example
 *
/**
 * Checks if a value is a plain object (an object created by the Object constructor or with a [[Prototype]] of null).
 * 
 * This function determines whether the provided value's prototype is the same as the default Object prototype,
 * indicating it's a plain object rather than an instance of a custom class or built-in type.
 * 
 * @param value - The value to check
 * @returns `true` if the value is a plain object, `false` otherwise
 * 
 * @example
 *
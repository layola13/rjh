/**
 * Extracts the sign of a number
 * 
 * @param value - The number to extract the sign from
 * @returns The sign of the number: -1 for negative, 1 for positive, 0 for zero, or the original value if it's NaN
 * 
 * @example
 *
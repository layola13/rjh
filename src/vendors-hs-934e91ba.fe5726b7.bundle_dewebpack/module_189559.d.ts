/**
 * 检查对象中指定键的值是否与给定值相等
 * 
 * @param value - 要比较的目标值
 * @param key - 对象中的键名(可以是字符串或数字索引)
 * @param object - 要检查的对象或类数组结构
 * @returns 如果对象是类数组且键在有效范围内,或对象包含该键且值相等,则返回true;否则返回false
 * 
 * @remarks
 * 此函数执行以下检查:
 * 1. 验证第三个参数是否为对象
 * 2. 根据键的类型(number/string)进行不同的验证:
 *    - 数字键: 检查对象是否类数组且索引在有效范围内
 *    - 字符串键: 检查键是否存在于对象中
 * 3. 使用严格相等比较对象中的值与目标值
 * 
 * @example
 *
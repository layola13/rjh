/**
 * 计算字符串中 Unicode 字符（包括表情符号、组合字符等）的真实长度
 * 
 * 该模块正确处理：
 * - 基本多文种平面字符（BMP）
 * - 补充平面字符（如表情符号）
 * - 组合字符序列（如带重音符号的字母）
 * - 肤色修饰符
 * - 零宽连接符（ZWJ）序列
 * - 区域指示符号（国旗表情）
 * 
 * @param input - 需要计算长度的字符串
 * @returns 字符串中实际可见字符的数量
 * 
 * @example
 *
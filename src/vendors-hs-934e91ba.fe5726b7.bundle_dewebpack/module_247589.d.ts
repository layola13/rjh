/**
 * Validates whether a given value contains circular references.
 * 
 * @param value - The value to check for circular references
 * @returns `true` if the value has no circular references, `false` if circular references are detected
 * @throws Re-throws any error that is not a CycleException
 * 
 * @example
 *
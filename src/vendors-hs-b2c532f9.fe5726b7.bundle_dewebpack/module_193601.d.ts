/**
 * Generates a UUID v4-like unique identifier string.
 * 
 * @returns A string in the format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
 * where each 'x' is a hexadecimal digit (0-9, a-f).
 * 
 * @example
 *
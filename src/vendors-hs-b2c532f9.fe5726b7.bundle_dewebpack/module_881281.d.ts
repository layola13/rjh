/**
 * Re-export module from the main implementation.
 * This module acts as a barrel export, delegating to module 94889.
 */

/**
 * Module exports interface.
 * Replace 'unknown' with the actual type exported by the main module.
 * Inspect module 94889 to determine the correct type structure.
 */
export type ModuleExports = unknown;

/**
 * Default export from the delegated module.
 * This should match whatever is exported from the main implementation.
 */
declare const moduleExports: ModuleExports;
export default moduleExports;
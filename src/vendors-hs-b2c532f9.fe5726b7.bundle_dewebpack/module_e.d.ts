/**
 * Error handler function that re-throws the provided error.
 * This is typically used in error handling chains or as a default error handler
 * that propagates exceptions up the call stack.
 * 
 * @param error - The error object to be re-thrown
 * @throws Always throws the provided error
 * @example
 *
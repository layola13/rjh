/**
 * Filters the current collection based on a selector, element, or function.
 * Reduces the set of matched elements to those that match the selector or pass the function's test.
 * 
 * @template T - The type of elements in the collection
 * @param selector - A string containing a selector expression, a DOM element, or a function to test each element
 * @returns A new filtered collection wrapped in a stack
 * 
 * @example
 *
/**
 * Module: module_has
 * 
 * Filters a jQuery collection to only include elements that contain at least one
 * element matching the specified selector or element(s).
 * 
 * @param selector - A string containing a selector expression, DOM element, or jQuery object
 * @returns A new jQuery collection containing elements that have descendants matching the selector
 * 
 * @example
 *
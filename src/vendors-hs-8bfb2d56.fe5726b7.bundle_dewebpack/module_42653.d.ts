/**
 * 阿里巴巴 Iconfont 图标组件
 * 
 * 该模块导出一个通过 Ant Design 的 createFromIconfontCN 方法创建的图标组件，
 * 用于加载阿里云 CDN 上托管的自定义图标字体。
 * 
 * @module IconFont
 */

import { createFromIconfontCN } from '@ant-design/icons';

/**
 * 自定义图标组件类型
 * 
 * 通过 Iconfont CDN 创建的 React 图标组件，
 * 可以通过 type 属性指定图标名称来使用。
 * 
 * @example
 *
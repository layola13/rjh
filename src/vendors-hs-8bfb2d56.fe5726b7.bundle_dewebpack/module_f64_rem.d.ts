/**
 * Computes the remainder of dividing two 64-bit floating-point numbers.
 * 
 * This function performs the modulo operation on two numbers, returning the 
 * remainder after division. The result has the same sign as the dividend.
 * 
 * @param dividend - The number to be divided (numerator)
 * @param divisor - The number to divide by (denominator)
 * @returns The remainder of dividend % divisor
 * 
 * @example
 *
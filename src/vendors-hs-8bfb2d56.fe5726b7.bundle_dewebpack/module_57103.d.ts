/**
 * Asset module that exports the URL path to the AIGC credits image.
 * This module represents a static asset reference typically handled by webpack's asset/resource loader.
 */

/**
 * The URL path to the AIGC credits PNG image asset.
 * This path is resolved relative to the application's public path and includes a content hash
 * for cache busting (7d71b46c).
 * 
 * @example
 *
/**
 * 将源数组的一段连续元素复制到目标数组的指定位置
 * 
 * @template T - 数组元素的类型
 * @param source - 源数组，从中读取元素
 * @param sourceIndex - 源数组的起始索引位置
 * @param count - 要复制的元素数量
 * @param destination - 目标数组，元素将被写入此处
 * @param destinationIndex - 目标数组的起始索引位置
 * @returns void - 该函数直接修改目标数组，无返回值
 * 
 * @example
 *
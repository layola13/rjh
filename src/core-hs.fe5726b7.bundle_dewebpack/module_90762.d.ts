/**
 * Array slice utility function
 * 
 * Extracts a section of an array and returns it as a new array.
 * Similar to Array.prototype.slice but implemented as a standalone function.
 * 
 * @param array - The source array to slice
 * @param start - The beginning index of the slice (inclusive)
 * @param end - The ending index of the slice (exclusive). If undefined, slices to the end of the array
 * @returns A new array containing the sliced elements
 * 
 * @example
 *
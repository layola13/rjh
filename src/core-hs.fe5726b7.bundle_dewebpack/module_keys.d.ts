/**
 * Iterator implementation that immediately returns done state.
 * This represents an empty iterator that has no elements to iterate over.
 * 
 * @module module_keys
 * @remarks Original module ID: keys
 */

/**
 * Creates an empty iterator that always returns done: true
 * Used when there are no keys or elements to iterate over
 * 
 * @returns An iterator object with a next() method
 * 
 * @example
 *
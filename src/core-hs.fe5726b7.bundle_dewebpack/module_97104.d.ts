/**
 * Type definitions for module_97104
 * 
 * This module is a minimal ES module that only sets up the ES module marker.
 * It exports nothing and contains no functionality.
 */

/**
 * Empty module that serves as an ES module marker.
 * This module doesn't export any values, types, or interfaces.
 * 
 * @remarks
 * The original Webpack bundle only contained the `__esModule` flag setup,
 * which is automatically handled by TypeScript's ES module compilation.
 * 
 * @module module_97104
 */
export {};
/**
 * External module declaration for webpack module 49604
 * 
 * This module represents an external dependency that is provided at runtime
 * by the webpack environment. The actual implementation is not bundled but
 * expected to be available in the global scope or through another means.
 * 
 * @module module_49604
 */

/**
 * The external module export.
 * 
 * Note: Replace `unknown` with the actual type of the external module
 * if known. Common types might be:
 * - A specific library type (e.g., `typeof React`)
 * - An object with known structure
 * - A function signature
 * - Any other TypeScript type
 */
declare const externalModule: unknown;

export = externalModule;
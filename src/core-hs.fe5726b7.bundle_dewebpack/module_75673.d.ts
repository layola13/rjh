/**
 * Creates a Set from an iterable collection by iterating over its elements.
 * 
 * @remarks
 * This function converts any iterable collection into a native Set by adding
 * each element individually. It leverages a forEach-like iteration utility
 * to process each element and add it to the resulting Set.
 * 
 * @param collection - The iterable collection to convert to a Set
 * @returns A new Set containing all unique elements from the input collection
 * 
 * @example
 *
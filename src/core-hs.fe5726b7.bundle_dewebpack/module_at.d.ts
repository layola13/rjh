/**
 * Array-like `at` method implementation
 * 
 * Returns the element at the specified index, supporting negative indices
 * that count from the end of the array-like object.
 * 
 * @param index - The position of the element to retrieve. Negative values count from the end.
 * @returns The element at the specified position, or undefined if the index is out of bounds.
 * 
 * @example
 *
/**
 * External module declaration for module 20198.
 * This module is provided externally (not bundled) and should be available in the runtime environment.
 * 
 * @module module_20198
 * @external
 */

/**
 * Type definition for the external module with ID 20198.
 * The actual implementation is provided by the webpack external configuration.
 * 
 * Replace `unknown` with the actual type if you know what this module exports.
 * Common patterns:
 * - If it's a library like React: `import('react')`
 * - If it's a specific class/function: define the interface below
 * - If it's a namespace: use `namespace` declaration
 */
declare const __WEBPACK_EXTERNAL_MODULE__20198__: unknown;

export = __WEBPACK_EXTERNAL_MODULE__20198__;
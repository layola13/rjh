/**
 * Validates that a value is an object (not a primitive).
 * This utility function ensures type safety by checking if the input
 * is a valid object reference before operations that require objects.
 * 
 * @param value - The value to validate as an object
 * @returns The input value if it is an object
 * @throws {TypeError} If the value is not an object (is a primitive)
 * 
 * @example
 *
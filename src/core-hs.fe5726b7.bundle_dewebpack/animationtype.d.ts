/**
 * Animation type enumeration
 * Defines the available animation transformation types
 */
export enum AnimationType {
  /**
   * Rotation animation type
   * Applies rotational transformations to objects
   */
  rotation = "rotation",
  
  /**
   * Translation animation type
   * Applies positional/movement transformations to objects
   */
  translation = "translation"
}
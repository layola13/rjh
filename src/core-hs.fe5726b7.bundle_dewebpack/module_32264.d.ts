/**
 * Gets the documentElement from the document object.
 * 
 * This module retrieves the root element (documentElement) of the document,
 * typically returning the <html> element in a browser environment.
 * 
 * @module module_32264
 * @returns The document's root element (HTMLElement), or undefined if not available
 */
declare const documentElement: HTMLElement | undefined;

export = documentElement;
/**
 * User Agent Detection Module
 * 
 * Provides the browser's user agent string for client environment detection.
 * Returns an empty string in non-browser environments (e.g., Node.js, SSR).
 * 
 * @module UserAgent
 * @example
 *
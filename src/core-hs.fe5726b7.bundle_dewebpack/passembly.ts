import { CabinetBase } from './CabinetBase';

export class PAssembly extends CabinetBase {
  constructor(
    firstParam: unknown,
    secondParam: unknown,
    thirdParam: unknown
  ) {
    super(firstParam, secondParam, thirdParam);
  }
}
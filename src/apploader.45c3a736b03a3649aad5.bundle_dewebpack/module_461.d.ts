/**
 * Extracts the second element (index 1) from an array or tuple.
 * 
 * @template T - The type of elements in the array
 * @param arr - The input array or tuple from which to extract the second element
 * @returns The element at index 1 of the input array
 * 
 * @example
 *
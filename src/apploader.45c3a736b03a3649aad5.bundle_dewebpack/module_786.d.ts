/**
 * CSS Module Type Definitions
 * 
 * This module represents a dynamically loaded CSS module with CSS Modules support.
 * The module injects styles into the document and exports CSS class name mappings.
 */

/**
 * CSS class name mappings exported by the CSS module.
 * 
 * When CSS Modules are enabled, this object contains mappings from
 * local class names (as written in the CSS file) to their globally-unique
 * generated class names (as used in the DOM).
 * 
 * @example
 *
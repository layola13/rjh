/**
 * 创建并显示内部网络检查提示框
 * 
 * @param message - 要显示的提示信息内容
 * @param isAnimated - 是否启用动画效果，false时显示左右箭头控制器
 * 
 * @remarks
 * 该函数会在页面body中创建一个提示框组件，包含以下特性：
 * - 自动移除已存在的同类提示框
 * - 可选的动画效果
 * - 可选的左右位置调节控制器（非动画模式下）
 * - 提示框初始位置为水平居中（left: 50%）
 * 
 * @example
 *
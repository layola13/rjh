/**
 * Webpack asset module that exports the public URL path for an SVG file.
 * This module represents a static asset reference that gets resolved at build time.
 * 
 * @module AssetModule
 * @description Originally module 915 - SVG asset reference
 */

/**
 * The public URL path to the SVG asset.
 * This path is resolved by the bundler and includes the content hash for cache busting.
 * 
 * @example
 *